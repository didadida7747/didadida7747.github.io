# LEARNING.md · 个人知识站开发复盘

> 项目：把「日常与规划」工作区（十几篇 Markdown 学习笔记）改造成一个带星空视觉、博客流、每日知识点和闯关游戏的个人知识站。
> 本文档面向**未来的自己**：假设你只记得基础语法，每一段都从"为什么"讲起。
> 术语第一次出现时附英文原文和一句话解释。

---

## 1. 技术栈总览

### 1.1 核心框架层

**VitePress**（静态站点生成器，Static Site Generator，简称 SSG——把源文件预先渲染成纯 HTML 文件的工具）
- **解决什么问题**：Markdown 文件 → 可浏览的多页面网站。侧边栏、全文搜索、目录大纲这些"文档站标配"它自带。
- **为什么用它**：本项目的内容 100% 是 Markdown，VitePress 的核心理念正好是"Markdown 即页面"——新加一个 `.md` 文件就自动成为一个网页，零胶水代码。它还内置了本地搜索（构建时把全部页面索引成一个小型搜索数据库，查询在浏览器里完成，不需要服务器）。
- **常见替代方案**：
  - **VuePress**：同门师兄，基于 webpack，启动慢，新项目已不推荐；
  - **Docusaurus**：React 系的 SSG，功能全但更重；
  - **Hugo**：Go 语言写的，构建速度极快，但页面逻辑要用 Go 模板写，做交互组件很别扭；
  - **Astro**：新兴的"内容站优先"框架，灵活度高，概念也更多。
- **取舍**：VitePress = 上手最快 + Vue 生态 + 自带搜索，代价是被绑在 Vue 生态里、主题生态比 Hugo 小。对"个人笔记站 + 一点交互"的需求，它是性价比最高的选择。

**Vue 3**（前端框架，负责页面里的交互逻辑）
- **解决什么问题**：星空动画、闯关游戏、学情分析这些**会变的东西**需要"数据一变、界面自动更新"的能力（这套机制叫**响应式**，Reactivity——你改数据，框架自动帮你改 DOM）。
- **为什么用它**：VitePress 的自定义组件只能用 Vue 写，这是它生态的"母语"。组件以**单文件组件**（Single File Component，SFC——把结构 `<template>`、逻辑 `<script>`、样式 `<style>` 写在同一个 `.vue` 文件里）形式组织。
- **替代方案**：React、Svelte、原生 Web Components。
- **取舍**：如果哪天换 Astro/Docusaurus，这些 `.vue` 组件要重写。但本项目核心资产是 Markdown 笔记本身，组件是壳，可承受。

**Vite**（构建工具，VitePress 底层自带，本项目没有直接配置它）
- **解决什么问题**：开发时秒级热更新；发布时把几百个源文件打包压缩成浏览器高效加载的少数几个文件。
- **替代方案**：webpack（老一代，慢）、Rollup（Vite 生产打包底层用的就是它）。

### 1.2 运行环境与数据层

**Node.js + npm**（JavaScript 的桌面运行时和包管理器）
- **解决什么问题**：浏览器外的 JS 执行环境——`npm run build` 那些命令就是 Node 在跑。
- **替代方案**：Bun（更快的新一代）、pnpm/yarn（更省磁盘的包管理器）。
- **本项目教训**：Node 版本要够新（本项目 24.x），旧版跑 VitePress 5.x 会有兼容问题。

**localStorage**（浏览器提供的本地键值存储，容量约 5MB，按域名隔离）
- **解决什么问题**：闯关游戏的最高分、答题记录、学情分析数据要"刷新不丢"。数据只存在你自己浏览器里，不上传任何服务器。
- **替代方案**：IndexedDB（异步、容量大、API 繁琐，适合大量数据）；服务端数据库（需要后端，本项目刻意 avoided）。
- **取舍**：同步 API 一行代码搞定，代价是换浏览器/清缓存数据就没了。对单人使用的工具站完全够。

### 1.3 视觉与交互层

**Canvas API**（画布——浏览器里一块可以用 JS 逐帧画图的区域）
- **解决什么问题**：星空背景有几百颗各自闪烁的星星 + 流星，用 CSS 得写几百个元素；Canvas 一个循环全搞定，性能更好。
- **替代方案**：纯 CSS 动画（简单但元素多时卡）；SVG（矢量清晰但同样多节点问题）；three.js/WebGL（3D 效果炸裂，但为一个星空引一个库太重）。

**CSS 自定义属性 + 过渡动画**（CSS Custom Properties，即 `--var` 变量；Transition/Animation）
- **解决什么问题**：品牌色（靛紫青）要同时作用于按钮、链接、标题、进度环——用变量定义一处、全局引用；深色/浅色模式切换就是换一组变量值。
- **踩过的坑**：见第 4 节"CSS 层叠覆盖"。

### 1.4 工程与工具层

| 工具 | 在本项目干什么 | 备注 |
|---|---|---|
| Git Bash / netstat / taskkill | 跑命令、查端口占用、杀残留进程 | Windows 下端口号被占是常事 |
| Node 脚本（`node -e`） | 一次性批量修 31 处锚点链接 | 别手改 31 处，写 10 行脚本 |
| 浏览器自动化（Playwright 风格 API） | 每轮改完做视觉验收 + 采样滚动轨迹 | 截图看效果、注入探针抓错误 |
| 开源仓库 + CC BY-NC-SA 协议 | 面试题手册的内容来源（huihut/interview） | 引用开源内容必须带出处和协议 |

**伪随机数生成器**（Pseudo-Random Number Generator，PRNG——用数学公式模拟"随机"的算法）
- **解决什么问题**："每日知识点每天不同、但同一天内固定"需要**可复现的随机**：拿日期字符串当**种子**（seed，随机序列的起点输入），同一种子必然产出同一序列。`Math.random()` 做不到这一点（不可指定种子）。
- **替代方案**：`crypto.getRandomValues()`（密码学安全，但同样不可播种，杀鸡用牛刀）。

---

## 2. 架构与数据流

### 2.1 一次页面访问的完整流程

以用户打开 `http://localhost:4173/计算机组成原理_全景导学笔记.html` 为例：

```
【构建期】（npm run build，一次性）
  工作区所有 .md ──→ VitePress 编译器（markdown-it 解析 Markdown，
                      Vue 编译器处理内嵌组件/HTML）
                   ──→ .vitepress/dist/ 下的纯静态文件
                      （每篇笔记一个 .html + 共享的 JS 资源 app.*.js）

【运行期】（用户浏览器）
  ① 浏览器请求 URL
       ↓ 由 VitePress preview 内置的静态文件服务器从 dist/ 目录返回
         （上线后这一步换成 GitHub Pages / Vercel 的内容分发网络，
          Content Delivery Network，CDN——全球分布的缓存服务器）
  ② 返回的 HTML 里已含完整正文 → 首屏立刻可见，不用等 JS
  ③ 浏览器加载 /assets/app.*.js，Vue 开始「水合」
       （Hydration：把静态 HTML "接活"——挂上事件监听和响应式数据）
       入口链路：.vitepress/theme/index.ts → MyLayout.vue
  ④ MyLayout.vue 的 onMounted 依次生效：
       - 注册滚动监听 → 离顶 80px 后给 <body> 加 .scrolled
         （导航栏、本页目录据此淡化）
       - 注册锚点点击拦截 → 目录点击变成平滑滚动
       - Starfield.vue 挂载 → Canvas 星空开始逐帧绘制
       - ReadingProgress.vue 挂载 → 右下角进度环开始响应滚动
  ⑤ 用户点侧边栏另一篇笔记：
       Vue Router 在浏览器内直接换页（不发服务器请求），
       route.path 变化 → MyLayout 的 watch 触发转场纱幕动画
```

### 2.2 三条核心交互数据流

**游戏答题流**（全部发生在浏览器内）：
```
点击选项 → QuizGame.vue 的 pick(i)
  → record(topic, 对错)：更新内存中的 stats 响应式对象
  → persist()：localStorage.setItem('quiz-stats-v1', JSON.stringify(stats))
  → computed 属性 analysis 依赖了 stats → 自动重算各主题正确率
  → Vue 检测到数据变化 → 学情条形图 DOM 自动更新
```

**每日知识点流**（为什么"每天不同、当天固定"）：
```
DailyKnowledge.vue 组件初始化
  → todaySeed()：把本地日期拼成 "2026-9-2" 字符串
  → knowledge.js 的 dailyPick(seed, 10)：
      日期字符串做种子 → 哈希打散 → 伪随机序列 → 从 30 条知识库不放回抽 10 条
  → 渲染列表，每条带「页面路径 + 标题锚点」的原文链接
  明天种子变成 "2026-9-3" → 完全不同的另一批；今天无论刷新多少次都一样
```

**快捷词搜索流**（一个跨组件协作的例子）：
```
点「指令」chip → DiscoverPanel.vue 的 openSearch('指令')
  → 用 DOM 点击导航栏的搜索按钮（VitePress 没公开搜索状态的 API）
  → 搜索弹窗是异步渲染的 → setInterval 每 100ms 检查弹窗输入框是否出现
  → 出现后：用「原生 setter」把词写进输入框 + 派发 input 事件
     （直接 input.value = x 不会通知 Vue，见第 3 节精读③）
  → VitePress 本地搜索（底层是 MiniSearch 索引，构建时生成）即时返回结果
```

### 2.3 目录结构及设计理由

```
日常与规划/                    ← 工作区根 = 站点内容源（关键决策！）
├── .vitepress/               ← 站点工程目录，浏览器永远不可见
│   ├── config.mts            ← 唯一配置：导航/侧边栏/搜索/排除/重写
│   ├── theme/                ← 自定义主题层
│   │   ├── index.ts          ← 主题入口：注册组件、指定自定义布局
│   │   ├── MyLayout.vue      ← 总布局：星空挂载、转场、滚动监听、平滑滚动
│   │   ├── Starfield.vue     ← Canvas 星空（纯视觉，无业务）
│   │   ├── DiscoverPanel.vue ← 首页搜索发现面板
│   │   ├── DailyKnowledge.vue← 每日知识点栏目
│   │   ├── ReadingProgress.vue ← 右下角阅读进度环
│   │   ├── QuizGame.vue      ← 闯关游戏（最大的组件）
│   │   ├── knowledge.js      ← 知识点数据库（Vue 组件和游戏共享的数据源）
│   │   └── custom.css        ← 品牌变量 + 全局排版
│   └── dist/                 ← 构建产物（发布就是上传这个文件夹）
├── public/                   ← 原样拷贝的静态资产（logo.svg → 网址 /logo.svg）
├── *.md、子目录/*.md          ← 内容源：每个 md 自动成为一个页面
├── node_modules/             ← 依赖包（不进版本库）
└── .gitignore                ← 告诉 Git 哪些不要管
```

**为什么工程目录必须在工作区根**（而不是套一层 `site/` 子目录）：
这是本次最大的架构返工。最初把 `package.json` 和 `node_modules` 放在 `site/` 里，构建时报错 `vue/server-renderer` 无法解析——因为 Vite 解析依赖时是**从源文件所在目录逐级向上**找 `node_modules` 的，笔记在根目录、依赖在 `site/` 里，向上走永远找不到。解决方案就是把整个工程提到根目录，让内容源和依赖"住在同一家"。教训：**工具链的目录约定优先于"看起来整洁"的目录洁癖**。

**为什么用 `rewrites` 而不是直接建 `index.md`**：工作区根的 `index.md` 容易和编辑器/其他工具的约定冲突，所以首页写作 `home.md`，再用配置里的 `rewrites: { 'home.md': 'index.md' }` 映射成网站首页。`每日视野简报/README.md` 同理映射成该栏目的首页。

**为什么 `knowledge.js` 单独抽成文件**：每日知识点栏目和闯关游戏都要用同一份知识点数据（游戏题是知识点自带的 `quiz` 字段生成的）。数据放一处，两个组件 import 同一个模块——这是"单一数据源"原则，避免改了知识点忘了改题库。

---

## 3. 核心代码精读

### ① localStorage 的 onMounted 守卫（QuizGame.vue）

```js
const stats = ref(EMPTY_STATS())   // 先用空数据初始化响应式状态

onMounted(() => {
  try {
    const raw = localStorage.getItem(STATS_KEY)
    if (raw) stats.value = { ...EMPTY_STATS(), ...JSON.parse(raw) }
  } catch (e) { /* 首次使用或数据损坏时静默降级 */ }
})
```

**在做什么**：把浏览器里存的答题记录读回来。`ref()` 是 Vue 的响应式容器——之后任何代码改 `stats.value`，页面上依赖它的部分会自动更新。

**为什么必须包在 `onMounted` 里**：VitePress 构建网站时，会在 **Node 环境**里预先执行每个组件的代码来生成 HTML——这个阶段**没有浏览器**，自然也没有 `window` 和 `localStorage`。如果直接把读取代码写在组件顶层，构建当场崩溃（`localStorage is not defined`）。`onMounted`（挂载后钩子）保证这段代码只在"浏览器里真实渲染之后"执行。这是 SSG 框架的头号经典坑，Nuxt/Next.js 用户一样会撞。

**更常见的写法**：纯客户端项目（比如用 Vite 裸搭的 SPA）可以直接顶层读取；也有框架提供 `onBrowserOnly` 之类的包装。这里多包一层 `try/catch` 是因为 localStorage 可能被浏览器禁用（隐私模式）、JSON 可能损坏——降级成空数据比白屏好。

### ② 日期种子的伪随机抽取（knowledge.js）

```js
export function dailyPick(seedStr, n) {
  let h = 2166136261                          // ① 哈希起点（一个大质数）
  for (let i = 0; i < seedStr.length; i++) {  // ② 把 "2026-9-2" 逐字符混入 h
    h ^= seedStr.charCodeAt(i)                //    ^= 按位异或：让每个字符影响最终值
    h = Math.imul(h, 16777619)                //    imul：32位整数乘法（防溢出失真）
  }
  const rand = () => {                        // ③ 每调一次，h 自我打乱一次
    h ^= h << 13; h ^= h >>> 17; h ^= h << 5  //    移位+异或 = xorshift 混沌迭代
    return ((h >>> 0) % 10000) / 10000        //    >>> 0 转无符号整数，再归一化到 0~1
  }
  const arr = [...KNOWLEDGE]                  // ④ 复制数组（不污染原数据）
  const out = []
  while (out.length < n && arr.length)
    out.push(arr.splice(Math.floor(rand() * arr.length), 1)[0])  // ⑤ 抽中即移走
  return out
}
```

**在做什么**：把字符串 `"2026-9-2"` 变成一串确定性的"随机"数，从题库里不放回地抽 n 条。

**逐段拆解**：
- ② 的循环是**哈希**（Hash——把任意输入映射成固定长度"指纹"的函数）思想：字符串每个字符都参与搅拌，改动一个字符结果天差地别。`Math.imul` 是关键——普通 `*` 在数超过 2⁵³ 时会丢精度，`imul` 强制按 32 位整数相乘，保证所有机器结果一致。
- ③ 是 **xorshift** 算法：数字不断"左移异或、右移异或"，产生看似杂乱实则可复现的序列。`>>> 0` 把可能为负的 32 位数转成无符号数。
- ⑤ `splice(i, 1)` 把抽中的元素从数组里**删除并返回**——这就是"不放回抽样"，同一条知识点不会一天出现两次。

**为什么不用 `Math.random()`**：它每次运行结果都不同，无法实现"同一天所有人看到同一批知识点"。种子随日期走，随机性（每天不同）和确定性（当天固定）就同时满足了。

**更常见的写法**：`seedrandom` 这类成熟库，或本项目另一处用的 `mulberry32` 算法（质量更高的 PRNG）。自写版本胜在零依赖、代码即教材。

### ③ 用"原生 setter"给 Vue 管理的输入框填词（DiscoverPanel.vue）

```js
const setter = Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, 'value').set
setter.call(input, word)
input.dispatchEvent(new Event('input', { bubbles: true }))
```

**在做什么**：往 VitePress 搜索弹窗的输入框里自动填入快捷词（比如「指令」），并让搜索结果立刻出现。

**为什么不能直接 `input.value = word`**：Vue 的 `v-model` 双向绑定靠的是**监听 input 事件**来更新内部状态——直接改 DOM 属性只是改了"显示的壳"，框架肚子里的状态没变，搜索索引根本不刷新。必须：① 改值 ② 手动补发一个 `input` 事件，框架才会"听到"。

**为什么绕一圈用 `getDescriptor(...).set`**：这是从 React 社区流传下来的标准技巧。某些框架会劫持输入框的 `value` 属性（在 setter 里做拦截），普通赋值可能被吞；从 `HTMLInputElement.prototype` 上取出**浏览器原生的 setter** 再调用，保证值落到真实 DOM 上。对 Vue 3 来说这一步其实可以直接赋值，但原生 setter 写法两边通吃，是值得记住的"跨框架保险"。

**`bubbles: true` 是什么**：让事件沿 DOM 树**向上冒泡**（Bubbling——事件从最深的节点逐层传给祖先）。监听器挂在上层容器，不冒泡就收不到。

**更常见的写法**：如果搜索组件是自己写的，直接暴露一个方法或共享状态即可，根本不用碰 DOM。这里走 DOM 是因为 VitePress 没有公开搜索框的内部状态——**调用第三方组件没有官方接口时的 DOM 级协作**，属于不得已但必备的技能。

### ④ 目录平滑滚动：捕获拦截 + setInterval 缓动（MyLayout.vue）

```js
// 第一部分：抢在框架之前拦下点击
document.addEventListener('click', onAnchorClick, { capture: true })

function onAnchorClick(e) {
  const a = e.target.closest('a')            // 从点击处向外找最近的 <a>
  if (e.defaultPrevented || ...) return      // 别人处理过就让路
  const href = a.getAttribute('href') || ''
  if (!href.startsWith('#') || href.length < 2) return   // 只管本页锚点
  const el = document.getElementById(decodeURIComponent(href.slice(1)))
  if (!el) return
  e.preventDefault()                         // 取消浏览器默认的"瞬移"
  smoothScrollTo(el.getBoundingClientRect().top + window.scrollY - 80)
  history.replaceState(null, '', href)       // 地址栏仍然更新（可后退、可分享）
}

// 第二部分：缓动滚动本体
scrollTimer = setInterval(() => {
  const t = Math.min(1, (performance.now() - start) / duration)  // 进度 0→1
  window.scrollTo(0, startY + dist * ease(t))                    // 当前该到哪
  if (t >= 1) { clearInterval(scrollTimer); scrollTimer = null } // 到站收车
}, 16)   // 16ms ≈ 每秒 60 帧

const ease = t => (t < 0.5 ? 4*t*t*t : 1 - Math.pow(-2*t + 2, 3) / 2)  // easeInOutCubic
```

**三个知识点**：
1. **事件捕获（Capture）**：DOM 事件传播分三段——捕获（从 `document` 一路向下到目标）、目标、冒泡（再回到 `document`）。默认监听在冒泡段；`{ capture: true }` 把监听挂到捕获段，就能**抢在 Vue Router 之前**看到点击，抢先 `preventDefault()`。本项目实测过：冒泡阶段拦截会被框架抢先处理。
2. **缓动函数（Easing）**：动画的本质是"每个时间点算出该在的位置"。`t` 是归一化时间（0=出发，1=到站），`ease(t)` 把匀速的时间轴弯成"慢-快-慢"的速度曲线——前半段 `4t³` 是加速，后半段是它的镜像减速。实测轨迹 `500→836→1824→4267→6915→8450→8998` 就是这条曲线的样子。
3. **为什么用 `setInterval` 而不是 `requestAnimationFrame`（rAF，浏览器专为动画提供的"每帧回调"）**：教科书写法一定是 rAF，但本项目在验证中发现：嵌入式/后台标签页里 rAF 会被浏览器**节流甚至挂起**，动画整个卡死；`setInterval(16ms)` 虽然老派，却不依赖"页面在前台"。**教训：教科书答案也要跑一遍真实环境。**

### ⑤ 一条 CSS 规则引发的"正文看不清"（custom.css）

```css
/* ❌ 事故版本 */
:root .vp-doc { color: #37415c; }      /* 本意：浅色模式下正文用墨色 */

/* ✅ 修正版本 */
html:not(.dark) .vp-doc { color: #37415c; }
```

**事故复盘**：深色模式的实现是给 `<html>` 追加 `class="dark"`，而 `:root` 选择器指的就是 `<html>` 本身——所以 `:root .vp-doc` 这个"后代选择器"在深色模式下**同样命中**。更致命的是它写在文件末尾：CSS 的**层叠（Cascade）**规则是"特异性（Specificity，选择器优先级的量化）相同时，后写的赢"。于是这条本该只属于浅色的墨色文字，覆盖了前面精心调的亮色正文——深字配星空底，用户看不清。

**修正**：`html:not(.dark)` 明确声明"仅当 html 没有深色 class 时生效"。

**通用教训**：写覆盖性样式前，先问三个问题——这条规则在**哪些状态**下会命中？它的**特异性**比被覆盖者高吗？它在**文件里的位置**靠后吗？三样全占才会真正覆盖。

---

## 4. 难点与踩坑记录

按时间顺序，共 12 个值得记录的坑。⭐ = 该技术栈的经典坑（以后大概率再遇到）。

| # | 问题现象 | 根本原因 | 解决方案 | 以后如何预防 |
|---|---|---|---|---|
| 1 | 构建报 `Element is missing end tag`，指向第三方仓库里的 md | ⭐ VitePress 把 Markdown 当 **Vue 模板**编译，克隆来的 openai-codex 仓库里的裸 HTML（`<div>` 等）被当成组件解析，缺闭合标签直接报错 | `srcExclude` 排除整个 `软件使用/grok使用/` | 第三方/机器生成的语料永远不要混进内容源；内容源里不写裸 HTML，必须写时整体包 `v-pre` |
| 2 | 构建报 `vue/server-renderer` 无法解析 | 依赖在 `site/` 子目录，源文件在根目录；模块解析从**源文件位置**向上找 `node_modules` | 整个工程迁到工作区根 | 项目初始就确定"根目录即站点"，不要中途套壳 |
| 3 | curl 测某些页面 404，浏览器却正常 | URL 编码差异：`+` 号、中文在 curl 与浏览器里的编码策略不同 | 用 `encodeURIComponent`/`encodeURI` 语义正确的工具重测 | 判断"页面挂了"之前，先用与浏览器相同的编码方式验证 |
| 4 | ⭐ 重建后 preview 服务器仍给旧页面 | preview 内置静态服务器启动时**缓存了文件清单**，不感知 dist 变化 | 每次构建后杀进程重启 preview | 养成"build → restart → 验证"的固定流程；或改用支持热更的 `npm run dev` |
| 5 | 重启时报 `EADDRINUSE: 4173 端口被占` | TaskStop 只杀了 shell 包装，node 子进程残留 | `netstat -ano \| grep :4173` 找 PID → `taskkill //F //PID` | 写启动脚本前先查端口；Windows 下杀进程要确认子进程 |
| 6 | 构建报 `EPERM: dist 无法删除` | preview 服务器正占着 dist 目录，Windows 文件锁 | 先杀 preview，再 build，再启动 | 固化为一条命令链：杀 → 建 → 起 |
| 7 | 快捷词点了弹出**空的**搜索框 | 搜索弹窗是**异步挂载**的，填词代码跑在输入框出现之前 | 轮询等待 `.search-input` 出现再填词 | 操作第三方组件的 DOM 前，先确认它存在（轮询/事件/MutationObserver） |
| 8 | 导航栏滚动淡化写了没生效 | 想象中的 `.is-scrolled` class **根本不存在**，实际是"离顶后移除 `.top`" | 改用 `:not(.top)` | ⭐ 给框架内部 class 写样式前，先用 devtools / `evaluate` 看**真实 DOM**，禁止凭想象写选择器 |
| 9 | 深色模式正文看不清 | 见第 3 节精读⑤：`:root` 前缀的浅色规则同样命中深色，且靠文件位置赢了层叠 | `html:not(.dark)` 限定作用域 | ⭐ CSS 层叠三问：命中哪些状态？特异性谁高？谁在后面？ |
| 10 | 31 条知识点锚点链接全部失效 | 手写的锚点按猜测的规则转写，而 VitePress 真实 id 规则是：**保留标点（`、·`）、数字开头加 `_` 前缀** | 从 dist 的 HTML 里 `grep` 出真实 id，写 node 脚本批量替换 31 处 | ⭐ 凡是**工具自动生成的标识符**（锚点、hash、类名），一律从产物里取真值，绝不手写猜测 |
| 11 | 平滑滚动"写了却不动"，反复时灵时不灵 | 三层原因叠加：① rAF 在嵌入式/后台标签页被节流挂起 → 换 `setInterval`；② 拦截器需挂**捕获阶段**抢先于 Vue Router；③ 验证环节的假信号——浏览器标签缓存旧 bundle、大纲**异步挂载**、冷启动深链水合失败 | 三者分别修复；验证时开全新标签、等大纲真实出现再操作 | ⭐ "代码不工作"时先证明**代码到底跑没跑**（hook 计数、console 打点），把"代码错"和"观察错"分开排查 |
| 12 | 编辑文件报 `File has been modified since read` | 环境里有格式化工具在写入后自动改文件 | 重新 Read 拿到最新状态再 Edit | 工具报"状态过期"不是故障，是保护；重读即可 |
| 13 | 所有页面的预渲染 HTML 变成空壳（`<div id="app"><!----></div>`），但浏览器里因为 JS 兜底渲染**完全看不出异常** | ⭐ 主题模块**顶层**写了 `let lastHash = location.hash`——SSR 构建期（Node）没有 `location`，模块导入即崩，VitePress 静默给每个页面渲染空壳；同时构建输出被 `tail -2` 截断，警告全被吞掉 | 浏览器全局对象的初始化全部移进 `onMounted`；排查时看完整构建输出 | 主题代码里 `window / document / location / localStorage` 绝不出现在模块顶层；调试构建问题时禁止截断输出——这次空壳 bug 能存活多轮，正是因为警告被自己的 `tail` 吞了 |

**如果只记三条**：
1. **构建期没有浏览器**——碰 `window`/`localStorage`/`document` 一律进 `onMounted`（坑 1、7 的共同根源都是"分不清代码跑在 Node 还是浏览器"）。
2. **别猜框架内部**——class 名、锚点 id、事件处理顺序，全部以真实 DOM 和产物为准（坑 8、10、11 的共同根源）。
3. **验证环境会骗人**——缓存、异步挂载、节流都会制造假象；先怀疑观察，再怀疑代码（坑 3、4、11 的共同根源）。

---

## 5. 安全与工程规范检查

**✅ 已做对的**：
- 无任何密钥/令牌入库（项目纯本地，没有第三方 API key）。
- `.gitignore` 已覆盖：`node_modules`、构建产物、`.bili_tmp/`（B站抓取的临时转写）、grok 克隆仓库。git 一旦初始化，这些不会泄露。
- localStorage 只存答题统计，`JSON.parse` 包了 `try/catch`，写操作也包了（防隐私模式下的配额错误）。
- 没有使用 `v-html`（XSS——跨站脚本攻击——的最常见入口），Markdown 内容全部是自己写的。
- 面试题手册引用开源内容时**注明了出处和协议**（huihut/interview，CC BY-NC-SA 4.0，个人非商业使用合规）。

**⚠️ 建议改进的**：

1. **公开部署前的隐私自查**（优先级高）：`每日视野简报/reports/` 会随站点公开。发布前通读一遍日报，确认没有个人信息、未公开的学校/项目细节。另外 `每日视野简报/data/raw/`（原始素材）目前只是被站点排除，**但没进 .gitignore**——若建公开仓库，建议一并忽略。
2. **`ignoreDeadLinks: true` 是止痛药不是治病药**：它让构建不因死链失败，初期合理；但也会把断链悄悄带上线。建议内容稳定后关掉它，让构建替你把关；至少定期手动跑一次"关掉它"的构建当体检。
3. **依赖健康**：目前唯一依赖是 vitepress，非常干净。上线前跑一次 `npm audit` 和 `npm outdated`；之后每月一次即可。
4. **部署配置预留**：GitHub Pages 项目页需要 `base: '/仓库名/'` 配置（否则白屏），部署时记得加；`public/` 里的 CNAME 机制等要用时再配。
5. **项目还不是 git 仓库**：大量手工劳动的成果没有版本保护，一次误删就全没了。建议尽快 `git init` 并做首次提交——哪怕不发布，本地版本控制也是"后悔药"。

---

## 6. 优化空间与练手方向

### 6.1 如果重写一遍

**性能（Performance）**
- 构建时有 `chunk > 500 kB` 警告（单文件过大，首屏加载变慢）：可以配 `manualChunks` 把搜索索引、游戏等拆成按需加载的块。
- 星空 Canvas 全程逐帧重绘：可加 `document.hidden` 判断，切到后台就暂停绘制省 CPU；星星数量已按屏幕面积计算，这块是合格的。
- 平滑滚动未检查 `prefers-reduced-motion`（用户系统级"减少动态效果"设置）——转场动画检查了，滚动没检查，属于遗漏。

**可维护性（Maintainability）**
- `knowledge.js` 全靠手维护，锚点又必须和构建产物一致：应该写一个 Node 脚本，从各篇 md 的标题**自动生成**知识点骨架 + 自动校验锚点，挂进构建流程（这也是练习方向②）。
- `QuizGame.vue` 400+ 行，学情统计逻辑可以抽成独立的组合式函数（Composable，Vue 的逻辑复用单元）`useQuizStats.js`。
- 侧边栏/导航手写在 config 里，文档多了会维护不动：可脚本化从目录结构自动生成。

**用户体验（UX）**
- 学情分析只有正确率条形图，可以加"最近 7 天趋势"。
- 搜索快捷词是静态列表，可以按用户历史点击动态排序。
- 每日挑战可以加连续打卡天数（streak），提升坚持的动力。

### 6.2 练手功能（按难度排序，都是在这个项目上继续开发）

**练习题①（入门）：错题本 + 复习模式** 
在游戏结算页把错题存入 localStorage（新 key `quiz-wrong-book`），再新建一个 `wrongbook.md` 页面 + 组件：列出所有错题、对应知识点原文链接，并提供"只重做错题"模式。 
练到：新页面接入 VitePress、跨组件共享 localStorage 数据、列表渲染与筛选。

**练习题②（进阶）：知识点与锚点的自动校验脚本** 
写 `scripts/validate-knowledge.mjs`：读 `knowledge.js`，对每条记录请求 dist 里对应的 html，验证锚点 id 真实存在、`answer` 在 options 范围内、`link` 文件存在；有任何失败就 `process.exit(1)`。再把它挂进 `package.json` 的 `"build"` 脚本前面。 
练到：Node 脚本工程化、把质量检查固化进构建流程（这是真实公司里 CI 的雏形）。

**练习题③（实战）：部署上线 GitHub Pages** 
建仓库 → `git init` 首次提交 → 配置 `base` → 写 GitHub Actions workflow（push 后自动 build 并发布）→ 开启 Pages。上线后把网址发给同学看。 
练到：Git 基础、CI/CD（Continuous Integration / Continuous Delivery，持续集成/部署——代码推送后服务器自动构建发布的机制）、真实世界的域名与缓存问题。

---

## 7. Prompt 复盘

先给结论：这个项目能长成现在的样子，一半功劳在**迭代式提示词**——你没有试图一次说清所有需求，而是每轮基于真实使用体验给反馈。下面逐条复盘。

### 7.1 全部提示词清单与效果评价

| # | 提示词（摘要） | 类型 | 效果 | 一句话分析 |
|---|---|---|---|---|
| 1 | "这些文档能不能做成网站……玩法可以是华丽 UI/博客/游戏**啥的**，你觉得我的想法怎么样？**继续**" | 立项 + 征求意见 | ⚠️ 利弊参半 | 本项目唯一引发大返工的源头，7.3 详细拆解 |
| 2/3 | "继续"×N | 节奏控制 | ✅ 够用 | 零信息但无害；方向正确时是润滑剂，方向错误时会放大错误——至少偶尔换成"继续，重点把 XX 做完" |
| 4 | "这个网站跟正常的网站有什么区别？**我是小白**" | 学习提问 | ✅ 很好 | 自报水平让我把解释校准到正确深度——这是最容易被忽略、最宝贵的元信息 |
| 5 | "这种网站不需要钱对吧？**我怎么听说**域名有的要付钱" | 学习提问 | ✅ 很好 | 带具体困惑 + 信息来源（"听说"），解释能对症下药 |
| 6 | "背景别纯黑要星空；要转场动画；搜索**仿搜索引擎**，列'你可能感兴趣/每日热点'……不至于**像无头苍蝇一样**" | 功能需求 | ✅ 全场最佳之一 | 三个需求各带三件套：要什么 + 参照物 + 为什么（痛点）。参照物消除歧义，动机决定方案取舍 |
| 7 | 八条反馈合体（转场生硬/快捷词空弹窗/导航太亮/字体单一/进度条/侧边栏排序/每日知识点/双模式+学情），末尾问"要不要接 AI API……我没渠道，**简单就做，难就先不做**" | 混合反馈 + 需求 | ✅ 有效但过载 | 每条都有现象和期望，质量高；但八件事挤一条消息，逐条验证困难。末尾的 AI 之问是教科书级授权：**约束 + 许可 + 兜底**三件套 |
| 8 | "每日知识点要**最小粒度**+原文位置链接，**不是你直接挑一个文档就行了**；刁难题删掉，换成知识点题和面试模拟题（**你可以去网上找一下**，沉淀成文档）；背景字体调亮；导航去黑线" | 纠偏 + 新需求 | ✅ 全场最佳 | "不是 X，而是 Y"句式精准纠正粒度误解；网上找资料是清晰的任务委派。自此项目方向完全对齐 |
| 9 | "正文文字跟背景贴合的**我都看不清了**" | Bug 报告 | ✅ 够用 | 一句话但症状独特可复现（真正的锅在我，见第 4 节坑 9）。补上"哪个页面/深色还是浅色"会更快 |
| 10 | "本页目录再翻的时候**也需要**虚化处理" | 功能对齐 | ✅ 简洁 | "也需要"三个字复用前文语境，省字又明确 |
| 11 | "点击目录**立马跳转**，能不能有**加速度的滑动**过程，看着更好看" | 体验优化 | ✅ 简洁清晰 | 现象 + 期望 + 动机，一行字三要素齐全 |
| 12 | 复盘教学：8 节结构化指令 + 输出要求 + "**先跑第 1~6 节，七八条先不管**" | 任务委派 | ✅ 教科书 | 编号分节、受众设定、输出要求、范围切割——本文档就是它的产物 |

### 7.2 五条通用原则

1. **需求带参照物和动机**。不说"搜索要好用"，说"仿搜索引擎，像'你可能感兴趣/每日热点'那样，因为现在像无头苍蝇"（#6）。参照物消除歧义；动机让实现者在方案冲突时知道往哪偏。

2. **纠偏用"不是 X，而是 Y"，并给出粒度**。#8 的"不是你直接挑一个文档就行了，相当于直接把知识点列出来，后面缀上原文链接"是全场价值最高的一句话——它同时说清了"你理解错成什么"和"正确答案长什么样"，所以一次就改对了。

3. **约束、许可、兜底一起给**。"我没有 AI 渠道（约束）；觉得简单就直接做（许可）；难或不方便就先不做（兜底）"（#7 末尾）。授权越完整，来回确认越少，实现者敢做决定。

4. **多需求要编号，最好带优先级**。#7 塞了八件事，能全部落地纯属侥幸（恰好相互独立）。更稳的做法是编号列表 + "必须 / 想要 / 可延后"分级，交付后可以逐条核对验收。

5. **范围切割是控制成本的超能力**。"先跑第 1~6 节，七八条先不管"（#12）——明确"这次做什么、下次做什么"，防止一次性交付过长、重点稀释。这条同样适用于日常："先只做深色模式，浅色下次再说。"

### 7.3 全场最需改进的一条：立项提示词（#1），改写示范

原文三个问题：

- **征求意见和开工指令混在一起**："你觉得我的想法怎么样？继续"——先听分析，还是直接开干？两者混排，我选择了"边答边干"。
- **"玩法……啥的"完全开放**：技术选型（VitePress）、游戏形式、视觉风格全部由我单方面拍板。不是不能拍板，而是后来花了整整三轮（#7、#8、#9）才把方向纠偏到位——这些成本本可以在立项时用一句话省掉。
- **没有约束条件**：没说免费/本地/要不要上线/给谁看，我按最保守假设（纯本地、单人用）做了，碰巧猜对。

改写示范：

> 我的工作区 D:\ai资料\日常与规划 里全是 Markdown 学习笔记（十几篇，有子目录），我想把它变成个人知识网站。
>
> 【目标】文档就是网站主干，以后新增笔记要能自动变成网页，不想手动登记。
> 【必须有的功能】① 首页 + 分类导航 + 全文搜索；② 深色星空视觉风格（不要纯黑）；③ 一个学习小游戏，题目从我的笔记内容里出——具体形式请你给 1~2 个方案让我选。
> 【约束】纯静态、免后端和数据库；托管优先零成本。
> 【这次只要】先回答两件事：推荐什么技术栈、为什么；目录结构大概长什么样。我确认后再开始实现。

对比要点：改写版把**决策权和执行权分开**——先锁方案再动手。立项时 5 分钟的澄清，值后面 5 小时的返工。

（顺带：#8 的"背景善良"应为"背景暗"的笔误，靠上下文成功还原了。错别字通常能被语境救回，但**关键名词**——页面名、功能名、文件名——值得发前多看一眼。）

---

## 8. 检验

5 道题：3 道概念 + 2 道动手，覆盖本项目最核心的知识点。只出题；把答案发给我，我再逐题批改讲解。允许翻代码、查资料——查得到并且能讲明白，也算学会。

### 概念题

**Q1（SSG 与水合）** `QuizGame.vue` 里读 localStorage 的代码必须包在 `onMounted()` 里。
(a) 如果直接写在 `<script setup>` 顶层，`npm run build` 时会发生什么？为什么**构建的机器上**会缺这个 API？
(b) "水合"（Hydration）是什么？为什么用户有时会先看到完整内容、零点几秒后点击才有反应？

**Q2（CSS 层叠）** 复盘"正文看不清"事故。当时的两条规则：

```css
.dark .vp-doc { color: #d5daf0; }   /* 写在前面：给深色模式的亮色 */
:root .vp-doc { color: #37415c; }   /* 写在后面：本想只给浅色模式 */
```

(a) 说明这两条选择器的特异性（Specificity）各是多少，谁更高？
(b) 深色模式下 `<html>` 明明有 `class="dark"`，为什么 `:root .vp-doc` 依然命中了深色页面？
(c) 最终哪条生效由什么决定？修复时改成 `html:not(.dark)` 为什么能一劳永逸？

**Q3（事件流）** 目录平滑滚动的监听挂的是 `document.addEventListener('click', fn, { capture: true })`。
(a) 描述一次点击事件在 DOM 里的完整传播路径（三段，按顺序）。
(b) 不挂 `capture: true` 时，Vue Router 会先做什么，导致我们的拦截器"拿到事件时已经晚了"？
(c) `e.preventDefault()` 和 `e.stopPropagation()` 分别阻止什么？最终版代码为什么删掉了 `stopPropagation()`？

### 动手题

**Q4（响应式状态 + 持久化）** 在游戏开场页的学情分析区块加一个"清空学情记录"按钮：点击后 (1) 删除 localStorage 里的 `quiz-stats-v1`；(2) 把内存里的 `stats` 重置为空，且学情条形图和建议文字要**立刻**消失。请写出行级修改：动哪个文件、加什么代码，并解释为什么这样 UI 会自动更新。提示：注意 `stats` 是 `ref()`，重置时应该给 `.value` 整体赋值还是逐个改属性？两种写法有什么区别？

**Q5（数据源 + 锚点取真值）** 向 `knowledge.js` 新增一条《大学生向上社交行动手册》的知识点（主题自选，比如"不同对象要用不同策略"），要求：
(1) `text` 是一句脱离上下文也能读懂的最小知识点；
(2) 带正确的 `link` 和 `anchor`——**写出你获取锚点真值的具体步骤/命令**（不许猜）；
(3) 带合法的 `quiz` 字段（4 个选项 + 正确答案下标），并回答：这条数据会被站内哪**两个功能**分别消费？各自取用的是哪些字段？

（Q5 做对即顺手给站内知识库添了砖——内容合适的话我会直接合进站点。）

---

*答题方式：直接在聊天里发答案即可，我来批改。第 7 节如果你对自己的提示词习惯有不同看法，欢迎反驳——复盘的价值在讨论，不在定论。*
