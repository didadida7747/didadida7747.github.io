# LEARNING.md · 第四轮开发复盘（2026-09-13）：video-notes 流水线与科协暑培 2025 上站

> **读者**：未来的自己，以及新开对话接手的 AI 助手。
> **范围**：本轮只干了一件事——把「视频合集转写 → 学习文档」从手工作坊升级成工业化流水线（`video-notes` skill + 勘误积累表 + CJK 字数脚本 + 模板 v2 + 学习文档索引），并用它的**首次实战**处理了科协暑培 2025 合集 19 讲、发布上站。1 个提交（`54236fd`），0 次回退，3 次 build 失败（当场修复），1 次 push 被代理卡住（绕过）。
> **前情**：二轮复盘存于 git 历史（`git show 54236fd:LEARNING.md`），三轮复盘在 [LEARNING-3.md](./LEARNING-3.md)（本轮顺手修了它的一个 build 故障）。
> **配套**：[HANDOFF.md](./HANDOFF.md) 讲「怎么干活」，本文讲「为什么这样干」和「踩过什么坑」。

---

## 0. 一分钟时间线

| 阶段 | 干了什么 | 结局 |
|---|---|---|
| 抢救 | tmp 里只剩 10 份转写（记忆说 13 份，3 份已被清理），全部抢救入 `素材库/`，md5 去重、按 BV 号重命名 | ✅ 资产保住，但 2026 合集永久丢了 8 讲转写 |
| 打断 | 用户打断：「你先说清楚你准备整理的文档是啥」→ 复述后才知道真正目标是用流水线处理 `2025科协暑期培训/` | ✅ 避免了把 tmp 抢救当成主任务做完收工 |
| 基建 | skill（SKILL.md + count_cjk.py）、积累表（~160 条种子）、模板 v2、索引 | ✅ 一次成型（积累表首次写入损坏，重写修复） |
| 实战 | 19 份讲义 → 7 个子代理分批产出详注（26310 汉字）→ 综合导览层（3416 汉字） | ✅ 讲师名从 frontmatter 补齐，12 讲讲师存疑 |
| 上站 | 侧栏新栏目 → build **连败 3 次**（`{&#8288;{` 插值 ×2、裸 `<T>` ×5 处）→ 修复通过 → push 被代理卡 → `-c` 覆盖推送 | ✅ `54236fd` 上线 |

**最终形态一句话**：用户以后只需把转写丢进 `素材库/<合集名>/` 然后说「给这个合集做导览」，流水线产出「薄导览层 + 逐集详注查阅层」两层文档并自动走完上站流程；勘误词和文档索引跨合集滚动积累。

---

## 1. 技术栈总览

### 1.1 VitePress（本轮加深了一层理解）

**VitePress** 是静态站点生成器（Static Site Generator，SSG：把源文件预先渲染成纯 HTML，访问时不需要服务器算数）。本站 v1 起就用它，但本轮踩到了它更底层的机制——**md 文件会先被当作 Vue 组件模板编译**（Vue：一个前端框架，模板里 `{&#8288;{ }}` 表示"插值"，即把变量的值填进去）。这意味着：

- 正文里出现字面量 `{&#8288;{`（哪怕包在行内代码反引号里）可能被当成插值开头，找不到结束符就报 `Interpolation end sign was not found`；
- 正文里出现 `<T>` 这类泛型尖括号（TypeScript/Rust 的写法），可能被当成 HTML 标签，报 `Element is missing end tag`。

**常见替代方案**：Hugo（Go 写的，纯文本处理，不会有这个问题，但主题生态对中文支持弱）、MkDocs（Python 系，样式朴素）、Docusaurus（React 系，重）。**取舍**：VitePress 胜在 md 即页面、自动侧栏、中文排版好、和 npm 生态一体；代价就是 md 不是"纯文本"而是"Vue 模板"，这个坑本轮实打实挨了三次。

### 1.2 ZCode 子代理（Agent 工具，general-purpose）

**子代理**（subagent）：主对话可以派生一个独立上下文的 AI 实例去干活的机制。它解决的核心问题是**上下文窗口**（context window：模型一次能读入的文本总量上限）——19 集转写每份 2.5 万～13 万字节，全部灌进主对话必炸。本轮的用法是「两步走」的工业化版：每个子代理读一份转写 → 直接把详注写成文件 → 只向主对话返回十几行的报告（概括 + 新勘误词 + 跨集联系）。主对话全程只碰摘要，不碰原始转写。

**常见替代方案**：①把转写切块分多次对话处理（人肉累、状态易丢）；②RAG 检索式（对"逐集完整总结"这种全文性任务不合适）；③长上下文模型硬读（贵且质量随长度衰减）。**取舍**：子代理方案质量最好、成本可控，但**输出契约必须写死**——本轮有一批子代理没按格式写"勘误对照"独立小节（把勘误直接融进正文了），合并积累表时只能以返回报告为准，这是契约不够硬的教训。

### 1.3 skill 机制（skill-creator）

**Skill**：把一段可复用的工作流程写成 `SKILL.md`（YAML frontmatter 的 `name` + `description`，正文是操作手册），AI 在遇到匹配场景时自动加载执行。放在 `~/.agents/skills/` 是用户级（跨项目可用），放在 `<项目>/.agents/skills/` 是项目级。`description` 是触发器，要写"什么场景该用我"而不是"我是什么"。本轮把六步流水线写进了 `~/.agents/skills/video-notes/SKILL.md`，配套脚本放同目录 `scripts/` 下。

**取舍**：skill vs 直接把流程写进记忆（memory）——记忆是"事实记录"，skill 是"操作手册"，后者能被结构化加载、可以带脚本资产，适合流程；前者适合记结论。本轮两者都用了：流程进 skill，资产路径和踩坑进记忆。

### 1.4 Python（标准库：re / pathlib / sys）

只用了标准库写了一个 60 行的 CJK 字数统计脚本。为什么不用现成的：`wc -m` 统计的是**字符数**（包括标点、空格、拉丁字母），对"目标字数按汉字数计"这个需求就是错的。**替代方案**：`wc -m` + 事后心算折扣（不可靠）、awk（对 UTF-8 多字节处理一堆坑）、正则计数（最直接）。**取舍**：一个 60 行脚本换一个可长期复用的度量基准，值。

### 1.5 Git（本轮用到的进阶用法）

- **`git -c http.https://github.com.proxy= push`**：`-c` 可以在单条命令上临时覆盖配置，值为空 = 禁用该项。本轮用它绕过了仓库里写死的 per-URL 代理（per-URL proxy：只对特定域名生效的代理配置）。
- **`.gitignore`**：声明哪些文件不进版本库。本轮新增 `素材库/` 和 `2025科协暑期培训/`，落实"转写原文不上站"的版权原则。
- **md5sum**：内容哈希去重，识别 ` 1.md` 这类副本哪份是完整版。
- **git show `提交:文件`**：从历史版本里取旧文件（二轮复盘就是这样保留下来的）。

### 1.6 Bash / Git Bash（Windows 上的 Unix shell）

本轮踩了两个经典 shell 坑（详见 §4）：`python -c "..."` 双引号内的反引号被解释为**命令替换**（command substitution：shell 会先执行反引号里的内容再把结果拼回去），解法是改用 **heredoc**（`<< 'EOF'`，把引号内原文整块喂给程序，引号要带 `'` 才不做变量展开）；Git Bash 下中文文件名显示为八进制转义（`\347\251\215...`），是 `core.quotepath` 的显示行为，不影响实际操作，想看中文可 `git config --global core.quotepath false`。

### 1.7 GitHub Pages + 代理

部署链路是 push 到 `didadida7747.github.io` 仓库，GitHub Pages 自动构建发布。本轮的坑：仓库配置了 `http.https://github.com.proxy=127.0.0.1:7897`（Clash 类代理工具的本地端口），代理客户端没开时 push 直接连不上 GitHub，但系统直连是通的——此时用 `-c` 覆盖即可，不必去改配置文件。

---

## 2. 架构与数据流

### 2.1 一次「合集 → 上站」的完整数据流

以本轮为例，用户发起：`2025科协暑期培训/` 里有 19 份讲义转写，处理后上站。

```
【素材准备层】(一次性)
用户粘贴/下载转写 (B站剪藏 md：frontmatter + iframe 播放器 + ## Transcript 全文)
  → cp 入 素材库/科协暑培2025合集/            [Bash cp / md5sum 判重 / 按 BV 号重命名]
  → 字节数扫描，<2KB 标记疑似缺转写            [skill 第 1 步「盘点素材目录」]

【流水线执行层】(每个合集一轮)
第 2 步 读全局资产
  → 积累表_转写勘误.md (四节词表)              [主对话 Read]
  → 索引_学习文档.md (哪几集已有蒸馏笔记)       [主对话 Read]
第 3 步 逐集子代理摘要 ×19
  → Agent(prompt=详注格式契约+积累表路径+读者画像)
    子代理: Read 转写 → Write 科协暑培2025_逐集详注/NN_短标题_BV号.md → 返回报告
  → 主对话只收报告（概括/新勘误词/跨集联系），不收转写
第 4 步 优先级校准
  → 已有蒸馏笔记的集降级略读、时效强的升精看     [主对话，依据索引与积累表]
第 5 步 综合导览层
  → 科协暑培2025合集_大二学生学习文档.md        [主对话 Write]
     （全景总纲/五线地图/总表/逐集要点/编者思考/附录勘误引用）
第 6 步 校验与回写
  → python count_cjk.py --strip-appendix …     [scripts/count_cjk.py: count_file() 正则计数]
  → 新勘误词 cat >> 积累表                     [追加「科协2025 各讲」三小节]
  → 索引「待处理」行移入主表                    [Edit 索引_学习文档.md]

【上站层】
.gitignore 挡素材库                            [.gitignore 新增两行]
侧栏登记                                       [.vitepress/config.mts: nav + sidebar 两组]
npm run build                                  [VitePress: md → Vue 编译 → HTML，含死链检查]
git add → commit → push                        [本次绕过 per-URL 代理]
GitHub Pages 自动部署 → 读者访问静态 HTML
```

### 2.2 目录结构及理由

```
D:\ai资料\日常与规划\            ← 工作区根 = 站点根（md 即页面）
├── .vitepress\config.mts       ← 站点引擎：nav/sidebar/rewrites/构建开关
├── 素材库\<合集名>\*.md|txt     ← 转写原文，gitignore，版权不上站，只喂子代理
├── 科协暑培2025_逐集详注\*.md   ← 查阅层，多文件栏目独立成目录，侧栏折叠组
├── 科协暑培2025合集_大二学生学习文档.md ← 导览层，单文件放根目录（站内惯例）
├── 积累表_转写勘误.md            ← 全局资产：跨合集滚动追加的勘误词表
├── 索引_学习文档.md              ← 全局资产：合集→产出→覆盖集数，优先级校准依据
├── 提示词模板_视频合集学习文档.md ← 模板 v2（人工兜底路径，与 skill 等价）
└── LEARNING.md / LEARNING-3.md  ← 复盘（本文 / 三轮）
C:\Users\fjbsllc\.agents\skills\video-notes\  ← skill 本体（用户级，不进任何 repo）
├── SKILL.md                    ← 六步流程手册
└── scripts\count_cjk.py        ← 字数基准
```

**为什么这样分**：①转写（版权物）与产出（自有笔记）物理隔离，前者 gitignore 后者上站，一条 `.gitignore` 规则守住原则，不靠每次小心；②查阅层多文件独立目录，导览层单文件，和 SI100+ 系列的既有组织方式一致（读者已习惯）；③积累表和索引用**平铺文件名**而不是塞进目录，因为它们是被跨合集高频引用的"注册表"，要在文件列表里一眼可见；④skill 放用户级目录，因为它和这个 repo 无关——下学期换个工作区还能用。

---

## 3. 核心代码精读

### 3.1 count_cjk.py：CJK 正则与"附录剥离"

```python
CJK_RE = re.compile(r'[\u4e00-\u9fff\u3400-\u4dbf\uf900-\ufaff]')
APPENDIX_RE = re.compile(r'^#{1,6}\s*(附录|附：)', re.M)

def count_cjk(text: str) -> int:
    return len(CJK_RE.findall(text))

def count_file(path: Path, strip_appendix: bool = False) -> tuple[int, int]:
    text = path.read_text(encoding='utf-8', errors='replace')
    total = count_cjk(text)
    body = total
    if strip_appendix:
        m = APPENDIX_RE.search(text)
        if m:
            body = count_cjk(text[: m.start()])
    return total, body
```

**它在做什么**：统计文档里的汉字个数。`\u4e00-\u9fff` 是 CJK 统一表意文字主区（CJK：中日韩，Chinese-Japanese-Korean），`\u3400-\u4dbf` 是扩展 A 区（生僻字），`\uf900-\ufaff` 是兼容区（同一个字的历史编码）。`re.M` 让 `^` 匹配每一行行首，所以「附录」标题不管出现在文档哪里都能定位到。

**为什么这样写**：`wc -m` 按字符数统计，会把标点、空格、英文单词全算进去——2026 科协文档用 `wc -m` 数会比真实汉字数高一截，而模板要求"正文 8000–10000 **汉字**"，口径必须统一。`findall` 返回所有匹配组成的列表，`len` 即个数，一行顶十行循环。

**更常见的写法**：用 `unicodedata` 逐字符判断 `category(ch) == 'Lo'`（Letter, other：CJK 汉字属于这个类别）也能做，但正则可读性更好、更快；也可以用第三方库 `jieba` 先分词再数，那是杀鸡用牛刀。`errors='replace'` 值得注意——遇到非法 UTF-8 字节不报错而替换成 `\ufffd`，对"计数"场景容错优先。

### 3.2 SKILL.md 的输出契约：子代理返回报告的格式

```markdown
## 返回报告（不要贴详注全文）
每个文件返回：文件名 | 一句话内容概括（≤40字）| 汉字规模（约数）|
你发现的新勘误词（转写→应为，每集最多 5 条，没有写"无"）。
最后附一段：三讲之间的联系（≤3 句）。
```

**它在做什么**：约束子代理的**返回值**——明确"报告里写什么、每项多长、不写什么"。最关键的是第一句括号里的"不要贴详注全文"。

**为什么这样写**：子代理的返回值会整个进入主对话的上下文。如果不限制，一个"热心"的子代理可能把 1500 字详注原样贴回来 ×19，主对话等于变相读了全部产出，两步走的隔离就被自己拆了。同理，「每集最多 5 条勘误」是防止子代理把低价值噪声（语气词误听）也塞进报告，污染积累表。

**有没有更常见的写法**：可以让子代理把报告写成一个 JSON 文件、主对话读文件，结构化程度更高，但多一步文件管理；对"十几个子代理、每个报告十几行"的规模，文本契约 + 明确上限已够用，且人读得懂。教训（本轮实际发生）：契约里没写死"详注文件必须包含独立的勘误对照小节"，结果 5 个子代理把勘误融进了正文——返回报告是对的（能合并），但文件格式不齐。**给输出格式列一份硬性清单，比描述性要求可靠。**

### 3.3 Vue 插值转义：`{&#8288;{`

```
esc = '{' + '&#8288;'        # &#8288; = 零宽不换行空格的 HTML 实体
t = t.replace('`{&#8288;{}}`', '`{' + '&#8288;' + '{}}`')
```

**它在做什么**：把 LEARNING-3.md 里行内代码中的字面量 `{&#8288;{}}` 和 `` `{&#8288;{` `` 改写成 `{&#8288;{`——`&#8288;` 是 HTML 实体（HTML entity：用 `&名字;` 或 `&#码位;` 表示特殊字符），8288 是"零宽不换行空格"（一个不显示、不占宽的字符）。

**为什么这样写**：VitePress 把 md 当 Vue 模板编译，`{&#8288;{` 是插值的开始标记；实测**行内代码反引号并不能完全保护它**（build 连续报 `Interpolation end sign was not found`，替换后立刻消失）。在两个花括号之间插入一个"不显示但合法"的字符，插值解析器就匹配不上 `{&#8288;{` 了，而渲染结果看起来一模一样。

**更常见的写法**：官方推荐 `v-pre` 指令（Vue 的"跳过编译"标记，给容器加 `v-pre` 则内部全部原样输出）。选实体插入是因为改动局部、不依赖容器结构，适合"一行里只有一个字面量"的场景。**预防之道**：上站前 `grep -n '{&#8288;{'` + grep 裸尖括号泛型，作为流水线第 6 步的固定动作。

### 3.4 Bash heredoc 替代 `python -c`

```bash
python << 'PYEOF'
import sys
sys.stdout.reconfigure(encoding='utf-8')
t = open('LEARNING-3.md', encoding='utf-8').read()
...
PYEOF
```

**它在做什么**：把 Python 代码整块喂给解释器。`<< 'PYEOF'` 是 heredoc（here document：把后续行当作标准输入直到结束标记），**引号必须带**——带引号的标记不做变量展开，`$`、反引号都安全通过。

**为什么这样写**：之前用 `python -c "..."` 时，bash 在双引号里遇到反引号就启动命令替换，试图把 `` `{&#8288;{}}` `` 当命令执行，报 `{&#8288;{}}: command not found`。教训：**`python -c` 的代码里只要同时出现单引号、双引号、反引号、`$` 中任意两个，就该换 heredoc**。`sys.stdout.reconfigure(encoding='utf-8')` 是 Windows 特有的必要动作：中文 Windows 控制台默认 GBK 编码（GBK：国家标准的汉字编码，ASCII 之外每字符两字节），print 中文或读 UTF-8 文件会甩 UnicodeEncodeError 或乱码。

**更常见的写法**：把代码写成 `.py` 文件再执行（最稳妥，值得长期用的就该这样，如 count_cjk.py）；`python -c` 只留一行以内的小操作。

### 3.5 git per-URL 代理的一次性覆盖

```bash
git -c http.https://github.com.proxy= push origin main
```

**它在做什么**：push 时临时把「github.com 专属代理」这一项配置覆盖为**空值**（空 = 该项失效），仅本条命令生效。

**为什么这样写**：仓库配置里有 `http.https://github.com.proxy=http://127.0.0.1:7897`——这是给"只有走代理才能连 GitHub"的环境准备的。当代理客户端没开但网络本身通时，Git 会去连一个不存在的本地端口然后超时。改 `.git/config` 是持久修改，会影响以后；`git config --global --unset` 动的是全局；`-c` 覆盖是**最小作用域**原则（改动只存在于这一条命令的生命周期）的教科书案例。

**更常见的写法**：用环境变量 `https_proxy= git push`（对 https 传输同样有效，但它覆盖的是环境而不是配置，与仓库里 per-URL 配置的优先级关系容易记混）。`-c` 写法的好处是明确对准了出问题的那一条配置。

---

## 4. 难点与踩坑记录

| # | 问题 | 根本原因 | 解决方案 | 以后如何预防 | 经典度 |
|---|---|---|---|---|---|
| 1 | 记忆说 tmp 有 13 份转写，实际只剩 10 份 | tmp 是粘贴临时目录，会被清理；记忆记录的是"当时"不是"现在" | 抢救剩余 10 份入素材库；skill 写死"转写必须落盘素材库" | 重要素材进工作区并入库（或至少留本地副本）；记忆里的路径要先验证再用 | ★★★ 临时存储当仓库 |
| 2 | 同一合集转写散在 4 个目录、文件名 `pasted-text-20260901-xxx` 无语义 | 粘贴落盘的文件名是时间戳，内容身份全靠人脑 | 提取 iframe 里的 BV 号 + 读开头时间戳识别主题 → `第N讲_标题_BV号.txt` 重命名 | 落盘时就用语义化文件名；BV 号是视频的全局唯一身份，用它当文件名锚点 | ★★ |
| 3 | OMOR 用户的"更新"实际是新增 ` 1.md` 副本，原 `.md` 仍是空壳 | 剪藏工具遇同名自动加 ` 1` 后缀，用户以为覆盖了 | md5 对比确认 ` 1` 版是完整转写，删空壳 | 收到"我更新了"先看 mtime 和文件列表，别假设改的是哪个 | ★★ 剪藏工具副本惯例 |
| 4 | 积累表首次 Write 落盘内容损坏（大量重复行、占位符） | 长文档单次生成的产出污染（生成长内容时的小概率故障） | 用源数据整体重写，`grep -c "corrupted\|占位"` 机械校验归零 | **写完必验**：新生成的长文件跑占位词/行数/编码检查再往下走 | ★★★ 长文本生成故障 |
| 5 | build 失败：`Interpolation end sign was not found` | md 里行内代码中的字面量 `{&#8288;{` 被 Vue 当插值开头（见 §3.3） | 插入 `&#8288;` 实体拆开 `{&#8288;{` | 上站前 `grep -n '{&#8288;{'` 全库扫描；**VitePress 的 md 不是纯文本，是 Vue 模板** | ★★★ 本技术栈标志性坑 |
| 6 | build 失败：`Element is missing end tag` | 泛型写法 `<T>` `<T, E>` `<int:article_id>` 被 Vue 当未闭合 HTML 标签 | 给裸尖括号包行内代码反引号（代码 span 内不编译） | 同上，grep 裸 `<字母` 扫描；写含类型参数的内容默认包反引号 | ★★★ 同一坑的第二张脸 |
| 7 | `python -c "..."` 报 `{&#8288;{}}: command not found` | 双引号内的反引号触发 bash 命令替换（见 §3.4） | 换 heredoc `<< 'PYEOF'` | 代码含引号/`$`/反引号组合时禁用 `-c` | ★★★ shell 引用地狱 |
| 8 | 12 讲讲师名缺失 | 剪藏时该视频源就没有讲师信息 | 从正文开场白挖（挖到 13 讲梁改正），12 讲实在没有 → 标存疑 | 元数据缺失宁可标"存疑"不编造——积累表铁律 | ★ |
| 9 | 导览层正文 3416 汉字，低于模板目标带 8000–10000 | 字数带按 2026 合集（13 集×1.5h 深课）校准；2025 是 19 集×1h 浅课，地图更宽更浅 | 接受现状（详注层承担深度），记忆里记"字数带按合集深度调整" | 字数目标应是**推导值**（基线/集 × 集数 × 深度系数），不是常数 | ★★ 模板参数化不足 |
| 10 | 一批子代理的详注缺独立"勘误对照"小节 | 输出契约写的是格式模板，没把"必须有哪些小节"列成硬清单 | 合并以返回报告为准补齐积累表 | 契约用"必须包含以下小节"清单式写法，不用"结构如下"描述式 | ★★ |
| 11 | push 连不上 GitHub（127.0.0.1:7897 拒绝） | 仓库 per-URL 代理写死，代理客户端没开；系统直连其实通 | `git -c http.https://github.com.proxy= push` 一次性禁用（见 §3.5） | `git config --get-regexp proxy` 知道自己仓库有什么配置；代理设开机自启 | ★★ |
| 12 | Edit 报 "File has been modified since read" | 两次 Edit 之间用 Bash 改了同一文件，工具状态跟踪失效 | 重新 Read 再 Edit | 同一会话改文件尽量走同一条通道，混用先 Read 刷新 | ★ |
| 13 | 导览层自查发现繁体「條」、`DeepEdge`（应为 DropEdge）、`[12:xx]` 占位时间戳 | 综合多个子代理报告时的手误 + 偷懒占位 | 逐个修复；占位时间戳回详注原文核实 | 产出后清单自查：占位符（xx/TODO）、错别字、术语与积累表一致性 | ★★ |
| 14 | Git Bash 中文文件名显示 `\347\251\215...` | git `core.quotepath` 默认把非 ASCII 路径转八进制显示 | 纯显示问题；介意就 `git config --global core.quotepath false` | 知道是显示层行为，别被吓去做无谓的编码转换 | ★ |

---

## 5. 安全与工程规范检查

**✅ 做对了的**：

1. **版权隔离（本轮最重要的安全动作）**：转写原文（B站 UP 主的劳动成果 + AI 字幕）不上站。`.gitignore` 新增 `素材库/` 与 `2025科协暑期培训/`，push 前用 `git status` 复核素材库确实被忽略。这不是新原则（公众号精选目录早已同样处理），但这次把它固化进了 skill 流程，以后每个合集自动执行。
2. **无密钥入库**：本轮所有提交内容仅 md 文档与配置。GitHub 仓库是公开的（github.io 用户页），这点要永远当默认前提。
3. **依赖零新增**：Python 只用标准库，前端无新包，供应链风险面没有扩大。
4. **本地 build 先验再 push**：三次 build 失败全部在本地拦截，没有把死链/编译错误推上线。

**⚠️ 需要改进的（按优先级）**：

1. **【中】frontmatter 里的 B站追踪参数**：素材库剪藏文件的 `source` 字段带着 `vd_source=cde74779a773c724d870ea7d8ec3ae30`——这是**用户的 B站身份追踪 ID**（tracking parameter：URL 里标识"谁分享/谁剪藏的"的参数）。目前因素材库不上站而安全，但这是埋着的雷：哪天把剪藏原文件直接上站就会泄露。建议：skill 加一条"引用 source 链接时只取 `bvid=` 参数，其余 query 全剥"。
2. **【中】push 即部署，无预览环节**：GitHub Pages 对 main 分支 push 直接发布。目前靠本地 build 兜底；个人站可接受，若要更稳可启用 branch preview（先推 preview 分支看一眼再合）。
3. **【低】代理配置的可用性**：`.git/config` 里写死 per-URL 代理，代理客户端挂了 git 就挂。二选一：代理工具开机自启；或移除这条配置、改在 shell profile 里按需 export `https_proxy`。
4. **【低】count_cjk.py 的输入校验**：文件不存在时打印提示继续跑、不报错退出，批量统计时一个路径打错会静默拉低总数。建议缺失文件计数并在最后以非零码退出。

---

## 6. 优化空间与练手方向

**如果重写一遍**：

- **性能**：①19 个子代理分 7 批串行派发，全程约半小时；可先发一个"素材体检"并行探明全部时长/讲师，再按讲次长短均衡分批。②侧栏 19 条详注是手写进 config.mts 的，下个合集又得手抄——应改成 `buildAutoSidebarGroup` 式的自动扫描（该函数已在 config 里，模式现成）。
- **可维护性**：①积累表用 `cat >>` heredoc 追加、没有查重，迟早撞重复——应有 `append_errata.py`（读详注勘误节 → 与积累表比对 → 只追加新词）。②skill 里四条资产路径是硬编码绝对路径，换机器就断——应收进一个 `assets.json` 统一引用。③导览层字数目标应参数化（基线/集 × 深度系数），而非模板里的死区间。
- **用户体验**：①导览层总表的讲次没有站内链接，读者要跑去侧栏点详注——总表讲次应直链详注页。②详注之间没有上一篇/下一篇导航，19 集连读体验断裂——前置配置 `prev/next` 即可解决。③导览层可给"精看 6 集"加合计时长估算，帮读者排期。

**练手题（按难度升序，都能在本项目上真跑）**：

1. **【易】`append_errata.py`**：解析详注的「勘误对照」表格，与积累表条目级查重（归一化转写列后比对），只追加新词并打印"新增 N 条、跳过 M 条"。练：正则、文件 IO、查重逻辑。
2. **【中】侧栏自动生成**：写一个 Node 脚本在 build 前扫描 `科协暑培2025_逐集详注/`，读每份第一行标题，生成 sidebar 片段注入 config.mts（参照现成的 `buildAutoSidebarGroup`）。练：Node fs、配置的函数化改造。
3. **【难】流水线断点续跑**：素材目录旁维护 `.progress.json`（已处理集/已回写积累表/已上站三个布尔位），skill 每步先查后跑，中断后从断点继续。练：状态机设计、幂等性（idempotency：同一操作重复执行结果不变的性质）——这是把脚本从"能跑一次"升级到"可以天天跑"的关键一步。

---

## 7. Prompt 复盘

本轮用户的提示词按时间序：

| # | 提示词 | 效果 | 分析 |
|---|---|---|---|
| 1 | 「读取 project-video-notes-skill-plan 记忆，开始构建 video-notes skill」 | ⚠️ 引发本轮最大一次方向校正 | 引用了记忆（好，给了准确锚点），但"开始构建"让我按记忆计划直接开工，而用户真正想要的下半场（处理 2025 目录）没说。我按记忆跑了很久 tmp 抢救，直到用户打断。**记忆是背景，不是今天的任务书**——没说清这一点是提示词的问题。 |
| 2 | 「等一下，你先给我说你准备整理的文档是啥？」 | ✅ 本轮性价比最高 | 在我继续跑流水线前打断，成本一句话，避免的方向性返工不可估量。时机也好——不是等我做完，是发现苗头就问。 |
| 3 | 「tmp是啥？」 | ✅ | 澄清一个我顺口用的术语。这条让后续沟通少了所有黑话歧义——用户不装懂，反而效率更高。 |
| 4 | 「我让你处理的文档是D:\ai资料\日常与规划\2025科协暑期培训里面的，处理完后上传到网站上，听明白没，复述我的需求，我同意后在执行」 | ✅✅ 教科书级 | 四要素齐了：**具体路径**（杜绝猜）、**可验证完成标准**（上站）、**强制复述**（暴露理解偏差）、**等确认再执行**（方向错误拦截在开工前）。我复述时补上的"转写不上站""OMOR 空文件怎么办"两个边界，正是复述环节逼出来的共识。 |
| 5 | 「《OMOR分享》一份只有 748 字节…我更新了，你看一下；执行吧」 | ✅ 小瑕疵 | 确认+授权干净利落。瑕疵在"我更新了"实际是新增 ` 1.md` 副本（工具行为），不是覆盖——多花一轮工具调用才找到。教训：**描述操作结果时带上"实际发生了什么"（新建了什么/改了哪个）比只说意图更省双向时间**。 |

**通用原则（从本轮提炼）**：

1. **记忆/历史是背景，不是任务书**。新会话开工时，把"参考哪些背景"和"今天要干什么"分成两句话说清，否则 AI 会默认"从记忆里最像任务的地方接着干"。
2. **大任务前要一次复述确认**。复述逼双方把隐含假设（"处理"是什么、"上站"到哪、版权边界在哪）摊到明面上——本轮方向校正全靠这个机制，成本极低。
3. **路径、数量、完成标准三要素**。"2025科协暑期培训里面的"（路径）+"19 份"（数量可验）+"上传到网站上"（完成态），比"帮我处理一下那些文档"少一半来回。
4. **纠偏趁早，一句话就够**。发现方向不对，第一时间的"等一下，你先说说你要干啥"比事后改需求便宜一个数量级。
5. **不懂的术语当场问**。一条"tmp是啥"换来全程零黑话歧义；装懂的成本藏在后面每一次"我以为你知道"里。

**最差一条（#1）的改写示范**：

> 原版：「读取 project-video-notes-skill-plan 记忆，开始构建 video-notes skill」
>
> 改写版：「读取 project-video-notes-skill-plan 记忆，里面的建 skill 计划仍然有效。本次任务分两段：① 按该计划构建 video-notes skill 及配套资产（积累表/脚本/模板/索引）；② 完成后，用它处理 `D:\ai资料\日常与规划\2025科协暑期培训\` 下的 19 份讲义转写，产出学习文档并发布到网站。tmp 里抢救旧转写只作为顺手的素材整理，不是本次目标。先复述执行计划，我确认后开工。」

改动点：给了记忆一个**角色定位**（计划仍有效，而非任务本身）；把真正的目标（②）放在明面上；顺手关掉了 AI 最可能自作主张的方向（tmp 抢救）；把复述确认内置成流程。

---

## 8. 检验

3 道题，答案都在本文里。答得出，说明这篇复盘没白写。

1. **为什么行内代码反引号包住 `{&#8288;{` 仍然会炸 build？最省事的转义是什么？（§3.3）**——追问：想整段跳过 Vue 编译，官方机制叫什么？
2. **`git -c http.https://github.com.proxy= push` 里的空值是什么语义？为什么不用 `git config --unset`？（§3.5）**——追问：这个改动的生效范围有多大？
3. **本轮"两步走"流水线里，哪一步的输出契约不严导致了返工？如果要给子代理的输出格式提一条硬性要求，你会写什么？（§3.2 / §4 表 #10）**
