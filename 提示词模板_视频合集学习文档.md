# 提示词模板：视频合集 → 大学生学习文档（v2）

> **用法（v2 变化）**：每次只需替换【素材】【已有笔记清单】两个槽位；其余是默认值。
> **大合集两步走**：集数多（>10）或转写总量很大时，先逐集提炼成结构化摘要（一集一节），再用摘要（而非原始转写）跑本模板做统筹——统筹质量更高，出错只需重跑单集。（`video-notes` skill 已内置此流程。）
> **产出分层**：本模板产出"导览层"（薄，30–40 分钟读完）；更细的逐集详注另存为"查阅层"，两层各司其职。
> **勘误引用制（v2 变化）**：文末不再从零整理勘误表，改为**引用**《积累表_转写勘误.md》+ **追加**本次新词；正文照积累表静默修正。
> **字数（v2 明确）**：目标字数按 **CJK 汉字数**计（用 `count_cjk.py` 验证），不是 `wc -m`。

---

【素材】B站视频合集《____》的语音转写字幕，存放于目录：`素材库/<合集名>/`（每集一个文件，B站剪藏 md 格式：frontmatter + 播放器 iframe + `## Transcript` 全文）。
> 交付前检查：目录内文件数 = 合集集数；无 ` 1.md` 类重复；字节数过小的文件（<2KB）单独确认是否缺转写。

【已有笔记清单】与该合集相关的已有产出（用于优先级校准：已有蒸馏笔记的集降级为略读；避免重复建设）：
- 见《索引_学习文档.md》中该合集行；无记录则填"无"。
- 本次产出后**回写索引**。

【读者】大二 CS 学生，已学过 C 语言和 Python 基础（就是我本人）。

【用途】开学前快速导览：读完能建立合集全景、知道每集讲什么、判断哪几集值得精看；不替代看视频，细节留给逐集详注。

【任务】产出两层 Markdown 学习文档：

**导览层**（单文件，结构如下）：

一、全景总纲（先给地图）
   - 合集分几大块、各集内在联系、贯穿主线
   - 一张总表：每集 | 一句话内容 | 优先级（精看 / 略读 / 可跳过），附优先级依据

二、逐集要点
   - 每集 ≤6 条、≤200 字，忠实原文观点，不添加原文没有的结论
   - 标注章节名/时间点，便于回看定位
   - 与我已有知识重复的内容，明确标"可跳过"并给一句理由

三、编者思考
   - 每条必须冠以"编者补充"标记，与原文严格区分
   - 只写以下三类，每条注明对应集数；对不上类型的不写：
     a. 跨集联系——几集之间未被点破的呼应、递进或矛盾
     b. 行动线——开学前/学期中的时间线建议，落到"何时看哪集、做何事"
     c. 缺口——合集没讲、但以我的背景开学后会撞上的东西（工具/课程/习惯）

**查阅层**（每集一个 md，放 `<合集名>_逐集详注/`，命名 `NN_短标题_BV号.md`）：
   - 定位（该集是什么、在合集里的位置）
   - 内容脉络（按时间戳分块逐块要点，标可跳过）
   - 勘误对照（该集转写→应为，只列影响理解的）
   - 编者补充（同上三类中该集部分）

【格式】条理清晰，层级标题与表格优先；转写错别字按《积累表_转写勘误.md》静默修正；文末勘误节写"详见积累表"+ 本次新发现条目（不重复收录已有词）。

【篇幅】导览层正文 8,000–10,000 CJK 汉字（附录与勘误不计入；`python count_cjk.py --strip-appendix` 验证）。超出时优先压缩入门集，保住总纲与编者思考。查阅层单集 800–1500 汉字。

【上站】产出入 VitePress 站点侧栏（`.vitepress/config.mts`），`npm run build` 验证后 commit+push；**转写原文不上站**，素材库目录不入 git。
