# LEARNING.md · 二次开发复盘（2026-09-06）

> **读者**：未来的自己，以及新开对话接手的 AI 助手。
> **范围**：v1 建站复盘之后的完整第二轮开发——内容扩充 → 功能迭代（错题本/每日任务/知识点卡片/术语图鉴/情境练习）→ 导航重构 → 大回退 → 精简为「资料储藏室」→ 视觉修复。共 11 个提交、4 个 git 标签、3 次「以为要返工」和 1 次「真的返工」。
> **v1 建站复盘**（原作者注：同样值得读）已存于 git 历史，获取方式：`git show 0b3172e:LEARNING.md` 或 `git show backup-full-features:LEARNING.md`。
> **配套文档**：[HANDOFF.md](./HANDOFF.md) 讲「怎么干活」，本文讲「为什么这样干」和「踩过什么坑」。

---

## 0. 一分钟时间线

先看这张表，后面所有章节都在解释这张表里的事。

| 提交 | 干了什么 | 结局 |
|---|---|---|
| `0b3172e` | v1 基线：原站上线版 | 存档 |
| `f3fce3b` | 知识库 4 栏目 46 页 + 错题本 + 每日任务执行台 | ✅ 上线（后在 `d576705` 回退） |
| `523ee1e` | 求职研究 2026-09 更新（战略校准/方向实验 + `sync-career.sh`） | ✅ 上线，**存续至今** |
| `733050b` | 知识点卡片（10 张，首页卡片墙 + 弹层） | ✅ 上线（后被回退） |
| `bb587e7` | 术语图鉴 34 条 + 情境练习 12 题 + 导航三分流 | ✅ 上线（后被回退） |
| `cf55f05` | 功能页独立拆分 + ToolTabs | ✅ 上线（后被回退） |
| `63b65d3` | **[并行会话]** 浅色 UI 改版 + 文档课 + 主题色切换器 | ✅ 上线（后被回退） |
| `d576705` | **大回退**：回到 `523ee1e` 并移除每日任务 | ✅ 上线 |
| `a3d08fe` | 移除首页搜索发现面板 + 每日知识点 | ✅ 上线 |
| `c9f8742` | 移除知识闯关 + 错题本 | ✅ 上线 |
| `1c41976` | 侧栏透明化，融入星空 | ✅ 上线 |
| `9f31440` | 清理侧栏条目里的闯关字样 | ✅ 上线 |
| `c117e7a` | 导航栏毛玻璃实底 + 本页目录卡片面板 | ✅ 上线 |

**标签（tag，给某个提交起的永久别名）**：`stable-pre-cards`=`523ee1e`、`stable-post-cards`=`733050b`、`backup-full-features`=`63b65d3`（完整功能版，随时可整体恢复）。

**最终形态一句话**：`523ee1e` 的内容 − 每日任务 − 发现面板 − 每日知识点 − 闯关 − 错题本，加上「星空从侧栏、导航栏、目录面板全面透出」的视觉方案。站点定位：**纯资料储藏室**。

---

## 1. 技术栈总览

### 1.1 框架层（v1 已有，本次用到更深）

**VitePress**（静态站点生成器，Static Site Generator，SSG——把源文件预先渲染成纯 HTML 的工具）
- 解决什么：Markdown 文件 → 多页面网站，自带侧边栏/搜索/目录。
- 本次深化了四个能力：**rewrites**（路径重写，把 `嵌入式体系/README.md` 映射成 `/嵌入式体系/` 栏目首页）、**srcExclude**（排除不想编译的文件）、**页面级 frontmatter 覆盖**（在 md 头部写 `sidebar: false` 可无视全局侧边栏配置——功能页拆分就靠它）、**sidebar 键按路径前缀匹配**（键 `/` 匹配全站）。
- 替代：Docusaurus（React 系、重）、Astro（灵活、概念多）、Hugo（快但模板语言别扭）。
- 取舍：内容 100% 是 Markdown + 需要 Vue 写交互组件时，VitePress 仍是性价比最高的选择。

**Vue 3 单文件组件**（SFC，Single File Component）
- 本次用到：`defineProps`（组件接收参数）、`computed`（依赖数据自动重算）、**Teleport**（传送门——把弹层的 DOM 渲染到 `<body>` 根节点下，避免被父元素的层叠规则困住）。
- 替代与取舍同 v1：组件是壳，Markdown 内容才是资产，换框架的迁移成本可承受。

### 1.2 版本控制与部署层（本次的绝对重头）

**Git**
- 本次用到的进阶操作：**tag**（标签，给提交起的永久别名，当存档位用）、`git checkout <tag> -- .`（把某个标签的**文件树**——tree，某一时刻全部文件的快照——还原到当前目录和暂存区，但**不产生提交**）、`git rm`（从版本控制中删除文件）、用**新提交实现回退**（而不是 `reset --hard` + **force push**，强推——改写远端历史的危险操作）。
- 为什么用「新提交回退」而不是强推：这是公开仓库、多会话并行开发，强推会让所有协作者（包括另一台电脑上的自己）本地历史错乱；新提交保留完整历史，后悔成本为零，代价只是历史变长。
- 替代：不加版本控制直接靠复制文件夹备份（v1 之前的原始状态）——一次误删就全没了。

**GitHub Actions + GitHub Pages**
- push 到 main → 服务器自动构建 → 发布到 `didadida7747.github.io`。这种「代码推送后自动构建发布」的机制叫持续集成/部署（CI/CD，Continuous Integration / Continuous Delivery）。
- 替代：Vercel/Netlify（功能更多，但对纯静态站是杀鸡用牛刀）。取舍：GitHub Pages 零成本、与仓库一体，代价是构建环境在 GitHub 侧，本地构建通过不代表远端一定通过（本次没踩，但要记住）。

### 1.3 网络层（本次新增的学习区）

**Clash Verge / mihomo 内核**（本机代理工具）
- 解决什么：本机到 GitHub 的直连不通（连接被重置），所有 git push / curl 到 GitHub 的流量必须走本地代理端口 `127.0.0.1:7897`。
- 关键发现：Clash Verge 分两层——**GUI 程序**和**内核进程**（verge-mihomo.exe）。GUI 没开时内核也不在，但可以用命令行手动拉起内核：`verge-mihomo.exe -d <配置目录> -f <配置文件>`。这招在 GUI 不可用时救了三次急。
- 替代：VPN 服务（不可控）、SSH 协议走 22 端口（部分网络下可达，没试成）。取舍：代理进程不随开机自启，重启电脑后要手动拉起或正常启动 Clash Verge。

**git 的 TLS 后端**（TLS，Transport Layer Security，传输层安全协议——https 加密的实现）
- Windows 的 git 默认用 **schannel**（Windows 系统自带的 TLS 实现）——走代理时 TLS 握手会失败；换 **OpenSSL** 后端并强制 **HTTP/1.1** 就稳定了。四件套完整命令见第 4 节坑 2。⭐ 这是「Windows + 代理 + GitHub」的经典组合坑。

### 1.4 工具层

| 工具 | 本次干什么 | 备注 |
|---|---|---|
| bash + python 混合脚本 | 批量拷贝/脱敏/清死链（`sync-career.sh`）、批量改代码 | python 负责带断言的字符串替换，sed 负责单行替换 |
| 浏览器自动化（browser-use，Playwright 风格 API） | 每轮改完做视觉验收：截图、点击、读 localStorage、量元素几何 | 教训见坑 14 |
| `node --input-type=module -e` | 校验数据文件（术语条数、题目答案索引、联想引用有效性） | 数据文件也是代码，改完必须跑一遍 |
| grep / `git diff --name-status` | 安全终扫、回退对账 | 自动扫描后必须人工复核（误报见坑 16） |

### 1.5 CSS 视觉层（本次核心学习区）

- **层叠上下文**（stacking context，浏览器决定谁画在谁上面的机制）与 **z-index**（上下文中的层级号）：星空 canvas 固定在 `z-index: 0`，内容整体 `z-index: 1`，这是「星星透出来」的基础。
- **backdrop-filter**（背景滤镜，俗称毛玻璃）：**模糊的是元素背后已经画好的像素**。这个语义直接导致了一个反直觉的事故——它会把 2~3px 的星星亮点糊成均匀色块（坑 12）。
- **position: sticky**（粘性定位）：元素先随页面滚动，到达指定位置后「粘住」不动。右侧「本页目录」就是靠它固定在视口右侧的。

---

## 2. 架构与数据流

### 2.1 一次访问的完整流程（当前线上版）

```
【构建期】（本地或 GitHub Actions 上，一次性）
  .vitepress/config.mts 被读取：
    nav / sidebar（键按路径前缀匹配） / rewrites / srcExclude
    → 收尾执行 buildAutoSidebarGroup()：
       扫描根目录 *.md，凡没登记进 sidebar 的自动进「🗂 未归类」组
  每个 .md 经 markdown-it 编译 → .vitepress/dist/*.html

【部署期】（push 触发，一次性）
  git push origin main
  → GitHub Actions 按 .github/workflows/deploy.yml 在服务器上重新构建
  → 发布到 https://didadida7747.github.io/

【运行期】（每个访客）
  ① 浏览器 GET 页面 → GitHub Pages 的内容分发网络（CDN，Content Delivery
     Network，全球分布的缓存服务器）返回预渲染 HTML，首屏不需要 JS
  ② app.js 加载，Vue 开始水合（Hydration，把静态 HTML 接上事件与响应式数据）
  ③ MyLayout.vue 的 onMounted 依次生效：
       - 滚动监听 → 离顶后给 body 加 .scrolled
       - 锚点点击拦截 → setInterval 16ms 平滑滚动（v1 定下的规矩：勿改回 rAF）
       - Starfield.vue → 星空 canvas 开始逐帧绘制
       - ReadingProgress.vue → 右下角阅读进度环
  ④ 用户点侧边栏另一篇 → Vue Router 浏览器内换页 → 转场纱幕动画
```

### 2.2 当前主题的层叠图（谁在谁上面）

从底到顶：

| 层 | 元素 | 样式 |
|---|---|---|
| z-index 0 | `.starfield-holder`（fixed 全屏 canvas） | `pointer-events: none`，纯背景 |
| z-index 1 | `.VPContent`（全部页面内容） | 内容文字**无底色**，直接浮在星空上 |
| 同层 | `.VPSidebar`（左侧栏） | `background: transparent`——星星直接透出 |
| 同层 | `.VPNavBar`（顶部导航） | 页面顶部时透明；**滚动后毛玻璃实底** `rgba(15,19,46,.78)` + `blur(14px)` |
| 同层 | `.VPDocAsideOutline`（右侧本页目录） | **毛玻璃卡片** `rgba(22,27,62,.55)` + `blur(10px)`，带边框圆角 |

这张图就是「储藏室视觉方案」的全部：星星是唯一的背景，所有 UI 元素要么透明、要么毛玻璃，没有任何纯色块。

### 2.3 目录结构职责（当前版）

```
日常与规划/                ← 站点仓库根 = 内容源
├── .vitepress/
│   ├── config.mts        ← 唯一配置：nav/sidebar/rewrites/srcExclude/自动收录
│   ├── theme/
│   │   ├── index.ts      ← 主题入口：注册全局组件、指定 MyLayout
│   │   ├── MyLayout.vue  ← 总布局：星空挂载、转场、滚动监听、全部视觉层叠规则
│   │   ├── Starfield.vue / ReadingProgress.vue
│   │   └── custom.css    ← 品牌变量与排版（当前基本空载，视觉规则都在 MyLayout）
│   └── dist/             ← 构建产物（发布就是上传它）
├── 嵌入式体系/  STM32工作流/  AI学习/  求职研究/   ← 4 个脱敏拷贝的资料栏目
│   （各自 README.md 被 rewrites 成栏目首页）
├── 每日视野简报/          ← 日报归档（data/prompts/templates 被 srcExclude）
├── 软件使用/              ← Codex/OMP/ZCode 学习路线
├── scripts/
│   ├── rebuild.mjs       ← 杀旧预览→构建→起预览
│   └── sync-career.sh    ← 求职研究源文档→站点 的同步脱敏管道（可重复执行）
├── public/               ← 原样拷贝的静态资产（logo.svg）
├── home.md               ← 首页（rewrites 成 /）
└── *.md                  ← 内容页；根目录未登记的自动进「未归类」组
```

### 2.4 版本时光机

标签 = 存档位。恢复任意历史形态的标准动作：

```bash
git checkout backup-full-features -- .   # 把完整功能版的文件树还原到工作区
git diff --name-status backup-full-features HEAD   # 对账：哪些文件是后来新增的
# 处理完残留文件后 git add -A && git commit（不要 force push）
```

---

## 3. 核心代码精读

以下五段全是**当前线上活着的代码/流程**，按学习价值排序。

### ① `config.mts` 的自动收录机制——新文件零登记

```js
function collectSidebarLinks(items, set = new Set()) {
  for (const it of items) {
    if (it.link) set.add(it.link)                    // 收集侧边栏里已登记的链接
    if (it.items) collectSidebarLinks(it.items, set) // 递归处理子分组
  }
  return set
}

function buildAutoSidebarGroup(links) {
  const skip = new Set(['home.md', 'index.md', 'wrongbook.md'])
  const items = readdirSync(process.cwd())           // 列出仓库根目录的全部文件
    .filter(f => f.endsWith('.md') && !skip.has(f) && !links.has('/' + f.replace(/\.md$/, '')))
    .map(f => {
      let title = f.replace(/\.md$/, '')
      try {
        const m = readFileSync(f, 'utf8').match(/^#\s+(.+)$/m)  // 抓第一个一级标题当显示名
        if (m) title = m[1].replace(/[*`]/g, '').trim()
      } catch { /* 读不出就用文件名 */ }
      return { text: '🆕 ' + title, link: '/' + f.replace(/\.md$/, '') }
    })
  return items.length ? [{ text: '🗂 未归类笔记（新文件自动收录）', collapsed: false, items }] : []
}
```

**在做什么**：往根目录丢一个新 md，不用登记就自动出现在侧边栏「未归类」组里，显示名自动取文件里第一个 `# 大标题`。

**为什么这样写**：注意它在 `defineConfig(config)` **之后**执行——因为它要先拿到完整的手写 sidebar，算出「已登记链接集合」，再用目录扫描结果做**差集**。这个「先声明、后求差」的次序是整个机制的灵魂。`skip` 集合是例外清单：首页、游戏页这类不该出现在未归类里的文件。

**更常见的写法**：完全放弃自动收录，每个文件手写进 sidebar（文件少时更可控）；或写脚本从目录结构**生成**整个 sidebar 配置（文件多时）。本项目 46 页栏目走了手写路线，根目录散文件走自动收录——两条路线并存。

### ② MyLayout 的星空层叠——一次 CSS 语义课

```css
.starfield-holder { position: fixed; inset: 0; z-index: 0; pointer-events: none; }
.VPContent       { position: relative; z-index: 1; }

/* 侧栏：全透明（曾经是 rgba+blur，见坑 12） */
.dark .VPSidebar { background: transparent !important; backdrop-filter: none; }

/* 导航栏：滚动后毛玻璃实底（曾经是 opacity 0.35 淡化，见坑 13） */
.dark .VPNavBar:not(.top) {
  opacity: 1;
  background: rgba(15, 19, 46, 0.78) !important;
  backdrop-filter: blur(14px);
}

/* 本页目录：毛玻璃卡片（曾经是无底色浮字） */
.dark .VPDocAsideOutline {
  background: rgba(22, 27, 62, 0.55);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(170, 180, 210, 0.1);
  border-radius: 12px;
  padding: 12px 14px;
}
```

**在做什么**：canvas 星空固定铺满全屏（`position: fixed; inset: 0`，`inset` 是 top/right/bottom/left 四个方向的简写），`pointer-events: none` 让它不挡任何点击；内容整体抬到 `z-index: 1` 浮在星空上；侧栏全透明让星星透出来；导航栏和目录面板用「半透明深色 + 模糊」的毛玻璃。

**为什么这样写**：三个元素三种处理，各有理由——侧栏要星星（透明）；导航栏下面会滚过正文文字（必须毛玻璃挡住，坑 13）；目录面板要和正文视觉分区（毛玻璃卡片）。同一个「融入星空」目标，三个位置三种答案，**取决于每个位置后面滚过的是什么**。

**关键语义**：`backdrop-filter: blur()` 模糊的是**元素背后已经画好的像素**。星星是 2~3px 的亮点，被 `blur(12px)` 一糊，能量摊开就成了均匀色块——这就是「毛玻璃」和「看得清背景」天然矛盾的地方。想要「透出星星」就不能用大模糊；想要「挡住滚过的文字」就必须模糊。侧栏和导航栏需求相反，所以一个透明、一个毛玻璃。

### ③ `sync-career.sh`——脱敏同步管道

```bash
cp "$S/01_岗位方向/岗位地图.md" "$D/岗位地图.md"     # 第一步：拷贝并改名
# ...共 11 个文件...

sed -i 's/西电电子信息背景加成/电子信息背景加成/g; ...' 岗位地图.md ...   # 第二步：脱敏
perl -0pi -e 's|\[([^\]]+)\]\((?:\.\./|0[05-8])[^)]*\)|$1|g' *.md       # 第三步：清死链
echo "同步完成。请运行安全扫描确认。"                                     # 第四步：留一句人工终扫提醒
```

**在做什么**：工作区源文档（`D:\ai资料\实习\career_research\`）持续在更新，这个脚本把「同步到站点」固化成一条命令：拷贝改名 → sed 批量脱敏（校名/城市/个人语境）→ perl 批量把指向未上站文件的链接降级成纯文字 → 提醒人工终扫。

**为什么这样写**：源文档更新了三次，每次都手工改会漏。**把流程写成脚本的最大收益不是快，是「每次执行都一样」**——不会因为某次手滑漏删一个「西电」。注意它故意不自作主张做构建，最后一步留给人工——自动化的边界要画在「出错能被人看见」的地方。

**更常见的写法**：Node 脚本 + 规则表（把脱敏词表做成数组循环替换），可测可扩展——这是练习①的方向。sed/perl 一行流胜在零依赖，败在难调试（坑 15 的全角字符静默失败就是它）。

### ④ 回退工作流——用新提交而不是强推

```bash
git tag backup-full-features 63b65d3        # 第 0 步：先给现状打存档位
git checkout stable-pre-cards -- .          # 第 1 步：把目标版本的文件树还原到工作区
git diff --name-status stable-pre-cards HEAD | grep "^A"   # 第 2 步：对账！列出目标版本之后新增的文件
git rm -qf .vitepress/theme/TermsPage.vue ...              # 第 3 步：把新增文件显式删掉
# 第 4 步：清理引用（config/nav/首页），构建验证
git add -A && git commit -m "回退：..."      # 第 5 步：以新提交落地
git push origin main                        # 第 6 步：正常推送，不改写历史
```

**在做什么**：把站点回退到五个提交之前的状态，但**不删历史、不强推**。

**为什么这样写**：`git checkout <tag> -- .` 的语义是「把标签里**存在的**每个路径覆盖到工作区」——它**不会删除**标签里没有的文件。所以第 2 步的对账不可省：本次就靠它抓到了残留的 `docs.html`（并行会话文档课的产物，混进了构建产物）。第 0 步的存档标签同样不可省：回退是破坏性操作，先给自己留一张「回退之前的回退」的票。

**更常见的写法**：`git reset --hard <tag> && git push --force`。两步搞定，但改写公开仓库历史，且被删的提交只能靠 reflog 找回。个人站用强推其实也能活，但「新提交回退」是可以在团队环境里活的习惯——习惯按高标准养。

### ⑤ 功能页拆分——frontmatter 页面级覆盖

```markdown
---
layout: doc
sidebar: false
aside: false
outline: false
title: 术语图鉴
description: 用大白话讲清站内术语……
---

<ToolTabs />

# 🗂 术语图鉴
……
```

**在做什么**：六个功能页（术语/练习/闯关/任务/错题本/卡片）在 md 头部的**元数据区**（frontmatter，文件开头两行 `---` 之间的 YAML 配置）里声明 `sidebar: false`，让这些页面彻底脱离文档侧边栏，成为独立页面。

**为什么这样写**：先试过在 config.mts 的 sidebar 里加空数组键（`'/terms/': []`）——没生效，因为 sidebar 键按路径前缀匹配，`/terms/`（带斜杠）和实际路由 `/terms` 的匹配细节很微妙。而 **frontmatter 是页面级配置，优先级最高、语义最直白**：「这个页面不要侧边栏」写在页面自己头上，比藏在全局配置的第 N 个键里可维护得多。教训：能写在页面头上的覆盖，不要写进全局配置的分支里。

---

## 4. 难点与踩坑记录

按主题分组，共 16 条。⭐ = 该技术栈/环境的经典坑，以后大概率再遇到。

### 网络与部署组

| # | 现象 | 根本原因 | 解决 | 预防 |
|---|---|---|---|---|
| 1 | `git push` 直连报 `Connection was reset`，重试无效 | 国内网络对 GitHub 443 端口的干扰 | 全部 git 远程操作走本地代理 | ⭐ 记住这台机器的既定事实：push 必挂 `127.0.0.1:7897` |
| 2 | ⭐ 挂上代理后报 `schannel: failed to receive handshake`，换 OpenSSL 又报 `unexpected eof` | Windows git 默认 TLS 后端（schannel）与代理的 TLS 处理不兼容；且 HTTP/2 经代理易断流 | 四件套：`-c http.proxy=… -c https.proxy=… -c http.sslBackend=openssl -c http.version=HTTP/1.1` | 存成记忆/脚本，别每次现场试错 |
| 3 | 推送中途代理反复失效（TLS eof 连续十几次） | Clash 只有服务进程在跑，内核进程没起；或内核上游节点劣化 | 找到安装路径 `E:\clash\Clash Verge\verge-mihomo.exe`，用它自己的配置手动拉起内核；劣化时杀掉重启内核 | ⭐ 代理故障排查分层：端口通不通 → 内核起没起 → 上游通不通。电脑重启后手动内核会消失 |
| 4 | push 时把并行会话做了一半的功能一起带上线 | `git add -A` 会把工作区所有改动（包括别人的半成品）都暂存 | 按 `git status` 逐个路径暂存；半成品功能明确「不提交」并持续隔离 | 多会话并行时，每次提交前 `git status` 过目 |

### 多会话协作组

| # | 现象 | 根本原因 | 解决 | 预防 |
|---|---|---|---|---|
| 5 | config.mts 里同时有我和并行会话的改动，无法整文件提交 | 两个会话改同一个文件 | 「还原 HEAD + 重放」拆分法：把混合版存到 /tmp → `git show HEAD:文件 > 文件` → 只重放自己的改动 → 暂存提交 → 再把混合版拷回工作区 | ⭐ 同仓库多会话开发，改配置文件前先 `git status` 看有没有别人的未提交改动 |
| 6 | 回退时发现并行会话已上线的 UI 改版（63b65d3）会被连带撤销 | 回退锚点（523ee1e）选在它之前 | 向用户说明后果后按指令执行；完整版打 `backup-full-features` 标签随时可恢复 | ⭐ 回退前先跑 `git log --oneline` 数一遍「会被连带撤销的提交」，向用户复述 |
| 7 | 并行会话未提交的依赖改动（package.json 等）差点被回退覆盖 | `git checkout <tag> -- .` 会覆盖已跟踪文件的工作区版本 | 回退前把未提交文件拷到 /tmp 备份，回退后回填 | 破坏性操作前，把「不在 git 里保护范围的东西」先摘出来 |

### VitePress 与前端组

| # | 现象 | 根本原因 | 解决 | 预防 |
|---|---|---|---|---|
| 8 | ⭐ 构建报 `gray-matter engine "layout: doc" is not registered` | python 批量插入文本时把 frontmatter 首行 `---` 和下一行挤成了 `---layout: doc`，解析器把 `layout: doc` 当成了引擎名 | 修复首行；之后所有批量改文件的操作都断言行结构完整性 | ⭐ 用脚本改「有严格格式结构的文件」（frontmatter/YAML/JSON）时，改完立刻构建验证；替换字符串里永远带上下文换行 |
| 9 | ⭐ ToolTabs 构建期报 `Cannot read properties of undefined (reading 'data')` | SSR（服务器端渲染——构建期在 Node 里预渲染页面）阶段，`useData()` 返回的 route 对象结构和浏览器里不同 | 改用 `useRoute().path`（构建期稳定可用） | ⭐ 主题组件里区分「构建期可用的 API」和「只有浏览器才有的 API」；不确定就选更底层的那个 |
| 10 | 功能页的空 sidebar 键（`'/terms/': []`）没生效 | sidebar 键前缀匹配的细节问题（键带尾斜杠 vs 路由不带） | 改用页面级 frontmatter `sidebar: false`——语义直白、优先级最高 | 页面级能解决的覆盖，不放进全局配置的分支 |
| 11 | `/points` 详情页点击展开时弹层也跟着弹出来 | 弹层（Teleport 渲染到 body 的浮层）和内联展开共用同一个 `openId` 状态，两种模式都触发了弹层渲染 | Teleport 的 `v-if` 加上 `mode === 'grid'` 条件 | 共享状态驱动两种 UI 时，画一张「哪个状态影响哪块 UI」的图再写条件 |
| 12 | ⭐ 侧栏加了 `rgba+backdrop-filter: blur(12px)` 后变成纯色块，星星全没了 | backdrop-filter 模糊的是「背后已画好的像素」——2px 的星星亮点被 12px 模糊摊成均匀色 | 侧栏改纯透明（它的背后只有星空，没有滚过的正文） | 用 backdrop-filter 前先问：它背后是什么？要挡文字就得模糊，要透背景就不能模糊 |
| 13 | ⭐ 导航栏「滚动后淡化到 35% 透明」导致正文文字从横栏底下透出来，与导航文字重叠 | 淡化只降低了导航自身的不透明度，背景仍是全透明——正文照样透过它可见 | 改为毛玻璃实底（不透明度 0.78 + blur 14px），正文被模糊层遮住 | 「防遮挡」的正确姿势是给遮挡物一个半透明+模糊的底，而不是把自己变透明。用户报「挡到内容」时先分清是「谁挡谁」 |
| 14 | 回退后 dist 里残留 `docs.html`（已删功能的页面） | `git checkout <tag> -- .` 只覆盖标签里存在的路径，不删除后来新增的文件 | `git diff --name-status <tag> HEAD \| grep ^A` 对账后逐个 `git rm` | ⭐ 树还原操作必须配对「新增文件对账」，这是 checkout -- . 和 reset --hard 的核心差异 |

### 验证与环境组

| # | 现象 | 根本原因 | 解决 | 预防 |
|---|---|---|---|---|
| 15 | sed 批量替换「全角引号+emoji」的文本静默失败（没报错也没替换） | sed 对复杂多字节模式的匹配问题 | 改用 python `io.open` 读入 + `in` 断言 + `str.replace`，替换前先打印 `found: True/False` | ⭐ 涉及中文全角字符的批量替换，用 python 并加 found 断言，不用 sed |
| 16 | 安全终扫误报：「平均分」命中关键词「均分」 | 子串匹配的天然缺陷 | 人工复核每一处命中 | 自动扫描的命中列表永远过目一遍再下结论 |
| 17 | browser-use 标签页在两次调用间被重置；`tabs[0]` 拿到的是用户自己开着的本地标签 | 受控标签列表是动态的，用户也在用同一个浏览器 | 每次操作前 `tabs.list()` 并**按 URL 匹配**目标标签；截图超时就重试 | ⭐ 浏览器自动化的标签选择永远按 URL/标题匹配，不按位置 |
| 18 | preview 服务器给的是旧构建的页面（v1 坑 4 复现） | preview 启动时缓存文件清单，不感知 dist 变化 | 每次构建后杀进程重启（`npm run rebuild` 封装了这套动作） | ⭐ 「build → restart → 验证」三步缺一不可 |
| 19 | ⭐ 本文自己的「难点表格」把构建炸了：单元格里的行内代码含管道符时，表格分列先于行内解析执行，代码 span 被切成两半，代码里的尖括号标签从「代码」变成「真标签」，Vue 报「缺结束标签」且报在文末（行号对不上源文件，因为 md 编译成模板后行号有偏移） | markdown 表格的竖线分列发生在行内代码解析**之前** | 单元格内的竖线写成反斜杠+竖线转义；或避免在表格里写含竖线的命令 | ⭐ 表格单元格里写命令先扫一遍竖线；写完 md 当场构建，别攒到最后一起验 |

**如果只记四条**：
1. **公开仓库 + 多会话并行**是这个项目的常态——提交前过 `git status`，破坏性操作前打标签存档、列连带影响。
2. **构建期没有浏览器，浏览器里才有的东西构建期都没有**——主题组件里碰 localStorage/window 想都不想直接进 onMounted；连 `useData()` 的子字段都要挑构建期安全的。
3. **backdrop-filter 不是免费的「半透明」**——它模糊背后的一切；要星星就别模糊，要挡字就必须模糊。
4. **自动化执行 + 人工复核**——python 替换带断言、扫描命中过目、回退后对账，机器干粗活，人守关键判定。

---

## 5. 安全与工程规范检查

**✅ 已做对的**：
- 站点与仓库**无任何密钥**——纯静态站没有 API 调用，唯一的 `.env` 类风险在工作区（多智能体辩论项目的 key），且其 `.env` 已 gitignore、未进站点仓库。
- **隐私红线流程已固化并执行三次**：拷贝 → 扫描（校名/姓名/手机号/绝对路径/裸 HTML/双大括号模板语法）→ 构建产物终扫。三份含真实姓名、手机号、学校排名的文件从未进入仓库。
- **上站边界意识**：所有内容必须先拷进仓库再脱敏；第三方克隆仓库（claude-cookbooks 等）从未被 srcExclude 放行，而是只拷精选文件。
- **并行会话隔离**：公众号精选（含占位符 feeds 的半成品）和自学资源被持续隔离在未提交状态，没有为赶进度提前上线空栏目。

**⚠️ 建议改进的（按优先级）**：

1. **工作区里的隐私文件仍在原位**：`D:\ai资料\老师任务\2transformer+强化学习\复盘和回信\email-template-to-professor.md` 含真实姓名和手机号。它不在站点仓库里（安全），但建议你自行删除或把个人信息换成占位符——只要它存在，每次「从工作区拷内容上站」都是一次 risks。
2. **`ignoreDeadLinks: true` 仍是止痛药**：本次新增了 46 页 + 大量站内互链，建议每季度跑一次「去掉 ignoreDeadLinks」的构建体检，让死链浮出来。
3. **并行会话的依赖改动长期挂在工作区**：package.json 里的 `rss-parser`/`turndown` 依赖和 `wx:sync` 脚本未提交。等公众号流水线收尾时一起提交；如果那个功能彻底放弃，记得把 package.json 还原，避免「装了依赖但代码不在」的困惑。
4. **代理内核是手动拉起的孤儿进程**：电脑重启后会消失，届时 push 又会失败。两个选项：正常启动 Clash Verge（推荐），或把拉起命令存成 `start-clash.bat` 放桌面。
5. **文档与现实的同步**：本次大回退后 HANDOFF.md 已同步，但如果未来从 `backup-full-features` 恢复功能，记得 HANDOFF 里对应章节也要跟着回来——文档说谎比没有文档更糟。

---

## 6. 优化空间与练手方向

### 6.1 如果重写一遍

**性能（Performance）**
- 构建时的 `chunk > 500 kB` 警告（v1 遗留，来自搜索索引）：可用 `manualChunks` 拆分。但交互组件删光后站点已很轻，优先级低。
- 星空 canvas 没有做 `document.hidden` 暂停（v1 遗留）：切后台标签页仍在逐帧画星星，费电。
- 大站快照：46 页栏目全部进一个 sidebar 配置对象，搜索索引随之变大——目前无感，页面翻倍后再治理。

**可维护性（Maintainability）**
- 4 个栏目 46 页的 sidebar 是 130 行手写配置：栏目内加页要手工登记。→ 练习②的脚本化。
- 脱敏扫描是一串手工 grep 命令，散在对话里。→ 练习①：固化成脚本进构建流程。
- `sync-career.sh` 只覆盖求职研究栏目：嵌入式/STM32/AI 栏目的源文档若更新，没有对应管道。→ 推广成参数化的 `sync-section.sh <栏目名>`。

**用户体验（UX）**
- 长文页的「本页目录」条目很多（计组那页 20+ 条），720p 高度下会滚动——可以限制只显示二级标题，或加 max-height 内部滚动。
- 移动端（<960px）侧栏抽屉没有为透明样式专门调过——桌面验收做足了，小屏只抽查过一页。
- 储藏室定位后「找资料」只剩 Ctrl K 全文搜索。如果哪天觉得不够用，`backup-full-features` 标签里的术语图鉴其实就是最好的「储藏室索引」，整版可捡回——先想清楚再动手，别为加而加。

### 6.2 练手方向（按难度排序，都在这个项目上继续开发）

**练习①（入门）：内容安全扫描器**
写 `scripts/check-content.mjs`：扫描四个栏目目录，命中任一危险信号——隐私词表（西电/本人/导师 + 你自定义的）、`D:\ai资料` 绝对路径、行首裸 HTML 标签、双大括号模板语法——就打印文件+行号并以退出码 1 结束；挂进 `package.json` 的 build 脚本前面。练到：Node 文件遍历、把人工检查固化成 CI 雏形。
**验收标准**：往任何一个栏目文件里塞一个「西电」，`npm run build` 必须失败。

**练习②（进阶）：侧边栏生成器**
写 `scripts/gen-sidebar.mjs`：读四个栏目目录的文件列表和首行标题，自动生成 sidebar 配置片段（栏目 README 变「栏目导览」置顶），输出 JSON 供 config.mts 读取或直接打印粘贴。练到：配置即数据、代码生成代码。
**验收标准**：删除手写的「战略与实验」分组，运行脚本后侧边栏内容不变。

**练习③（进阶）：最近更新页**
写 `scripts/recent.mjs`：用 `git log --name-only` 找出最近 30 天有改动的页面，生成一个纯静态的「最近更新」md 页（页面名+日期+链接），挂进构建前。练到：用 git 历史做内容运营，全程零交互、不破坏储藏室定位。
**验收标准**：push 一次内容改动后，线上「最近更新」页自动多出对应条目。

---

## 7. Prompt 复盘

先给总评：这一轮的提示词整体质量很高——**定调类**（初始需求、储藏室定位）和**纠偏类**（两次结构性纠偏）都给出了关键信息；问题集中在**回退指令的目标矛盾**和**bug 描述的指代模糊**。

### 7.1 逐条点评（按时间序，摘要有删节）

| # | 提示词（摘要） | 评价 | 一句话分析 |
|---|---|---|---|
| 1 | 二次开发初始需求：先读两文档+现状+目标（工作区有价值内容）+三条约束 | ✅ 教科书 | 指定前置阅读、划定范围、给流程约束（大改动先方案/本地验收再 push/遵守硬规则）。后续所有协作的秩序都源于这条 |
| 2 | 确认方案 + 追加每日任务需求（描述自己的工作流，"不能的话也没关系，你自己决定"） | ✅ 优秀 | 追加需求带使用场景（我靠这个设计出贴任务→打卡→复盘草稿的闭环），且给足授权兜底 |
| 3 | 「网址还是原网址吗？」 | ✅ 简洁有效 | 关键部署焦虑一句话说清 |
| 4 | 「你操作吧」 | ✅ 高效授权 | 配合事前方案确认，授权边界清晰 |
| 5 | 「实习文件夹里加入了新的内容，你添加一下，协调原本的功能」 | ⚠️ 略模糊 | 「协调原本的功能」可作多种解读（当时工作区还有并行会话的半成品）；靠执行者自行判断，结果还行但属于运气 |
| 6 | vibe-hub 参考需求第一轮：术语卡片+发散联想+保留文档展示+「记住当前状态保证能回滚」 | ✅ 优秀（有隐患） | 参照物+新功能定义+回滚安全要求俱全；隐患在没说卡片数量和入口位置——我自作主张放首页，埋下返工 |
| 7 | 「首页要一直往下翻，臃肿……上面导航栏有术语，文档，练习……卡片要有 vibehub 所有功能+联想栏……数量既精华也不要少……练习学习模仿他」 | ✅ 方向清晰 / ⚠️ 引发大返工 | 这条本身写得好（现象+参照物+期望），但它和第 6 条的「以原来的网站为基础，不要破坏」存在隐性冲突——第一轮我选择了保守解释（卡片墙放首页、保留原导航），两轮后才对齐。**根因不在这条，在第 6 条没说清「旧东西哪些可以动」** |
| 8 | 「视野简报/知识库/学习工具不就是在文档里嘛……不要保留首页卡片墙；确认」 | ✅ 全场最佳纠偏 | 直接给出正确的目标结构（文档下拉收纳一切），一行顶十行 |
| 9 | 「把术语，文档，练习拆分一下……每个功能单独的 html，不要首页」 | ✅ 清晰（小歧义） | 「不要首页」有歧义（删首页 vs 去掉首页导航项），执行者赌了后者并赌对——但这类词值得多说半句 |
| 10 | 「记住当前状态，保证在改坏的情况下，能回滚到当前状态」（出现在第 6 条里，单独拎出来） | ✅ 极好 | 用户主动要求存档位，直接催生了 tag 工作流——安全意识是最好的需求 |
| 11 | 回退指令：引用旧消息 + 「回退到这里，这个版本回退到没有每日任务时的版本」 | ❌ 全场最需改进 | 两个问题：①引用的目标版本（523ee1e）**实际已包含每日任务**，与「没有每日任务」的描述矛盾；②没说明卡片/术语/练习/浅色 UI 这些后续功能的去留。执行者追问后未获回复，只能按最佳判断执行，结果中「并行会话浅色 UI 被连带撤销」是用户事先不知道的副作用 |
| 12 | 「不要搜索，猜你想看，站内热点，每日知识点，只留闯关+错题本……我只要这个网站变成我资料的储藏室就够了」 | ✅ 优秀 | 一句话完成功能取舍+站点定位，后续所有决策有了准绳 |
| 13 | 「知识闯关和错题本也删了」 | ✅ 极简清晰 | 站点定位已经确立，六个字就够 |
| 14 | 「左面的导航栏为什么背景是纯色的，跟右边的星空背景不一样」 | ✅ 好的 bug 报告 | 现象+位置+与期望的对比，三要素齐全 |
| 15 | 「本页目录四个字所在的那个横栏回挡到内容」 | ⚠️ 指代模糊 | 错别字（回挡→会挡）+「横栏」指代不明（导航栏？目录面板？）——执行者测了三种窗口宽度、量了元素几何才定位到真正的病灶（导航栏透字） |

### 7.2 五条通用原则

1. **描述目标状态，而不是描述路径**。第 11 条用「照着那条旧消息回退」当指令，但人对「那个版本有什么」的记忆会出错（523ee1e 里明明有每日任务）。直接说「我要的最终状态是：留下 X、删掉 Y」，让执行者去选路径。
2. **给参照物时，同时给差异清单**。第 6 条给了 vibe-hub 当参照（好），但没说「现有网站上哪些东西允许为它让路」（缺），导致执行者保守解释。参照物消除「做成什么样」的歧义，差异清单消除「可以动什么」的歧义——两个都要。
3. **破坏性操作前，让执行者复述影响范围**。第 11 条里如果有一句「先列出你会删什么留什么给我看」，浅色 UI 被连带撤销的意外就不会发生。回退、删除、覆盖类指令，默认带一句「先列清单再动手」。
4. **Bug 报告三要素：哪个元素 + 什么操作/状态 + 期望与实际的差异**。对比第 14 条（一次定位）和第 15 条（三轮排查）——「左栏背景是纯色」和「横栏挡到内容」之间差的就是这三要素。
5. **授权一句话 + 事前定边界**。本项目最高效的配合模式是「方案先行确认 + 你操作吧 + push 前再确认」：决策权、执行权、发布权三层分离，来回最少、风险可控。第 12 条的「站点定位」则证明：**一句定性（储藏室）比十条功能列表更能约束后续行为**。

### 7.3 最差一条的改写示范

原文（第 11 条）：
> [引用旧消息]……回退到这里，这个版本回退到没有每日任务时的版本

问题：引用版本的内存印象与事实不符；未说明连带影响是否接受；未说明后续功能的去留。

改写示范：
> 网站要回退。目标状态：
> 【保留】知识库 4 个栏目、求职研究 2026-09 的更新、错题本、深色星空主题。
> 【删除】每日任务执行台、知识点卡片、术语图鉴、情境练习、导航三分流。
> 【我不确定的】那个浅色 UI 改版（另一会话做的）在删除清单之后才上线——先告诉我它会不会被连带删掉，再动手。
> 【要求】用 git 标签回退，先列出删除/保留的文件清单给我确认，再执行和 push。

对比要点：**目标状态用「留下/删除」两组清单表达**（不依赖对历史版本的模糊记忆）；**把不确定的点显式抛出**（而不是让执行者猜）；**给执行加上确认闸门**。这三点正好对应这次返工的三个成本来源。

---

## 8. 检验

5 道题：3 道概念 + 2 道动手改代码，覆盖本次最核心的知识点。只出题；把答案发到聊天里，我逐题批改讲解。允许翻代码、翻 git 历史——查得到并能讲明白，也算学会。

### 概念题

**Q1（Git 回退策略）** 本次回退用的是 `git checkout stable-pre-cards -- .` + 显式 `git rm` + 新提交，而不是 `git reset --hard stable-pre-cards` + 强推。
(a) 为什么 checkout 方案执行完，工作区里还残留着 `terms.js`、`docs.md` 这些「目标版本里不存在」的文件？这条命令到底做了什么、没做什么？
(b) 两种方案最终都能让线上变成目标状态，各自的代价是什么？为什么这个项目选了前者？
(c) 如果回退后第二天又想恢复完整功能版（backup-full-features 标签），用本文的流程该敲哪几条命令？中间要防什么坑？

**Q2（CSS 层叠与 backdrop-filter）** 侧栏第一版写的是 `background: rgba(19,23,53,0.6); backdrop-filter: blur(12px)`，结果星星全看不见，侧栏成了一块纯色。
(a) backdrop-filter 模糊的到底是什么？为什么小亮点会被「糊没」而大片背景色反而看不出变化？
(b) 同样是毛玻璃，导航栏用 `rgba(15,19,46,0.78) + blur(14px)` 却是「正确答案」——它和侧栏的需求差异在哪？从「元素背后滚过的是什么」角度回答。
(c) 如果把侧栏改成 `background: transparent` 但删掉 `pointer-events: none` 的星空 canvas 会发生什么？（提示：想想 z-index 0 和 canvas 覆盖全屏）

**Q3（SSG 的构建期/运行期边界）** ToolTabs.vue 第一版用 `useData().route.data.relativePath` 判断当前页，本地 dev 模式正常，`npm run build` 却报 `Cannot read properties of undefined (reading 'data')`；换成 `useRoute().path` 后构建通过。
(a) 为什么同一个 API 在 dev 和 build 两种模式下行为不同？（提示：build 时每个页面在 Node 里被预渲染一遍，此时"当前路由"是什么？）
(b) 这和「localStorage 必须包在 onMounted 里」是同一个根源吗？说明异同。
(c) 列出两个你会在主题组件里写的操作，一个属于「构建期安全」，一个属于「必须等浏览器」。

### 动手题

**Q4（写一个安全扫描脚本）** 把本次散在对话里的手工扫描固化成 `scripts/check-content.mjs`：递归扫描 `嵌入式体系/`、`STM32工作流/`、`AI学习/`、`求职研究/` 四个目录的全部 `.md`，命中任一信号——隐私词表（至少含「西电」「本人」「导师」三个词）、`D:\ai资料` 绝对路径、行首以 < 开头的裸标签（如 div、p、br、img 的起始标签写法）、双大括号模板语法——就打印 `文件:行号: 命中内容` 并以退出码 1 结束，全干净则打印通过。写出完整代码，并回答：为什么它应该挂在 `npm run build` 前面而不是后面？

**Q5（改一条 CSS 并说清理由）** 用户现在觉得：滚动阅读时，右侧「本页目录」毛玻璃卡片存在感太强，希望它平时安静（降低到 30% 不透明度），鼠标悬停或键盘聚焦时恢复。请：
(a) 写出这段 CSS（提示：滚动状态由 MyLayout 脚本加在 body 上的 `.scrolled` class 标记；恢复条件要包含 `:hover` 和 `:focus-within`）；
(b) 说明为什么「安静态」只降 `opacity` 而不要把 `background` 改成 `transparent`（结合坑 12/13 的教训）；
(c) 这段 CSS 应该加 transition 吗？加的话写上，并说明 transition 放在哪个选择器上才能让「进入安静」和「恢复」都有过渡。

---

*答题方式：直接在聊天里发答案，逐题批改。第 7 节的 Prompt 复盘欢迎反驳——复盘的价值在讨论，不在定论。*
